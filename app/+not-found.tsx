/**
 * 없는 주소로 들어왔을 때 — 첫 화면으로 돌려보낸다.
 *
 * 이 파일이 없으면 Expo Router 가 개발자용 기본 화면(검은 바탕에 "Unmatched Route ·
 * Page could not be found")을 보여 준다. 그것은 개발자에게 하는 말이라, 기도하러 온
 * 사람에게는 앱이 고장 난 것으로만 보인다.
 *
 * 실제로 겪은 일이 이 파일을 만들게 했다. 웹 판을 아티팩트로 올렸더니 앱이
 * `/_f/1788902…/…` 같은 깊은 주소 아래에 놓였는데, 이 앱은 브라우저 주소를 그대로
 * 읽어 화면을 고르므로 그 주소에 해당하는 화면을 찾지 못했다. 앱은 멀쩡히 떠 있었으나
 * 사용자가 본 것은 검은 오류 화면이었다.
 *
 * 그래서 어떤 주소로 들어오든 첫 화면으로 보낸다. 주소를 남겨 두고 오류를 보여 주는
 * 것보다, 앱을 열어 주는 편이 언제나 낫다.
 */
import { Redirect } from 'expo-router';

export default function NotFound() {
  return <Redirect href="/" />;
}

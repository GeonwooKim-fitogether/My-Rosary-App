# My-Rosary-App — 프로젝트 안내

> 한 줄 요지: **MyRosary는 "바람 하나를 정하고 54일 동안 날마다 묵주기도를 바쳐 완주하게 하는" 한국어 묵주기도 앱**이고, 이 저장소는 그 앱을 iOS(App Store)와 Android(Google Play) 양쪽에 출시하기 위해 실제로 개발하는 곳이다. 상품기획은 atelier 저장소에서 끝났고, 정본은 2026-09-05에 여기로 이관됐다.

## 지금 어디까지 왔나

**관문 둘이 모두 닫혔다. 지금 열려 있는 것은 구현이다.** 관문 1(기획안 컨펌)은 2026-09-05에 닫혔고 — 화면 세 곳을 프로토타입 방식으로 가기로 해 화면이 여덟에서 열로 늘었다(결정 1) — 관문 2(시안)는 atelier v5 인계본이 정본 자리에 앉으면서 2026-09-08에 대체로 닫혔다(결정 2, `docs/design/v5/`). 같은 날 **프레임워크·스택이 A안(Expo + React Native + EAS Build + Supabase)으로 확정됐다**(결정 3). M0(바닥)·M1(기도 루프)·M2(여정과 홈)가 작업완료다 — Expo 골격과 도메인 코어 위에 v5 의 화면 여덟(로그인·기도·하루 완주·홈·새 기도·여정 상세·여정 완주·설정)과 초대 코드 화면이 서 있고, 여정과 설정이 기기에 저장되며, 바텀 시트 일곱과 밤 벌(쪽빛)이 v5 값에서 파생돼 붙었다(Q-14). 화면 사진은 `docs/plan/m1-screens/`와 `docs/plan/m2-screens/`에 있다. **실기기(iOS·Android) 확인은 아직 한 번도 하지 못했다** — 이 컨테이너에 맥도 폰도 없어 소리·진동·이어폰 단추는 사람이 확인해야 한다. **2026-09-17 에 시안 정본이 바뀌었다(결정 11)** — 공방장이 Claude Design 인계본 「MyRosary World」(`docs/design/world/`)로 최종 개발하기로 지목했다. 지역 다섯·언어 일곱·원형 고리 묵주·성화 갤러리를 갖고 계정과 조가 없는 시안이며, 개발 순서는 `docs/plan/roadmap-world.md` 의 W0~W5 가 정본이다(옛 M0~M5 를 대체). 시안이 침묵하던 여섯 자리(계정·조, 배포 형태, 언어 범위, 묵주, 낭송 척추, 판본·밤 벌)는 **결정 12 가 추천값 그대로 확정했다** — 계정과 조는 V1.5 로 미루고, 표면은 셋(iOS·Android·설치형 웹앱), 언어는 한국어와 영어 둘, 묵주는 시안의 원형 고리 기하에 재질 넷, 낭송은 이 저장소의 셋과 받는 사이를 시안 어법으로, 판본 시트와 밤 벌은 접는다. **같은 결정이 머지 승인도 위임했다** — 마일스톤 PR 은 자동 검사가 초록이고 노출 증명이 서면 Claude 가 머지하되, 돈·바깥 노출·라이브 데이터베이스·계획 변경 넷은 그대로 사람 관문이다. **W0(토큰·글꼴·언어 일곱·성화 표·설정)는 2026-09-17 에 main 에 들어갔다.** 지금 여는 것은 W1(기도 화면)이다. 현재 상태와 다음 할 일은 언제나 `decisions.md`와 `docs/plan/roadmap-world.md`가 정본이다 — 이 파일의 이 절은 요약일 뿐이니, 어긋나면 그 두 파일을 믿는다.

## 정본 문서가 어디 있나

| 무엇 | 어디 |
|---|---|
| 제품 요구사항(PRD 제3판, FR-01~45), 디자인 시스템, 서비스 설계, 화면 명세, 화면 지도 | `docs/product/06-*.md` — 원본은 atelier `idea/mobile-rosary/`, 이관 뒤 atelier 쪽은 freeze. 이관 경위는 `docs/product/README.md` |
| 시안 계보 (v5 이전) | `docs/product/prototype/` (atelier 최초 핸드오프) · `docs/design/screens-day.html` · `screens-night.html` · `prototype.html`. **정본이 아니다** — 값이 어긋나면 v5 를 따른다(결정 2) |
| **화면 시안 정본 (Claude Design 「MyRosary World」 인계본)** | `docs/design/world/` — `MyRosary World.dc.html` 에 화면 열, `data.js` 에 언어 일곱의 기도문·문구와 지역 다섯의 색 벌, `_ds/` 에 「Classical」 디자인 시스템, `img/` 에 성화 열일곱 장(표에 오른 것은 열여섯), `prompt.md` 에 시안을 만든 지시문. 여는 법과 v5 와의 차이는 그 폴더의 `README.md` (결정 11) |
| **개발 로드맵 정본 (W0~W5)** | `docs/plan/roadmap-world.md` — 살아남는 자산과 다시 그리는 것, 시안이 침묵하는 여섯 자리의 결정 카드(§3), 마일스톤 여섯과 노출 증명, 시안 자체의 결함 열 가지, 사람 몫 |
| 시안을 이 컨테이너에서 실제로 렌더한 화면 사진 | `docs/plan/world-screens/` — 열두 장, 390×844. 열 장은 대체 글꼴로 찍혔고(Q-49), 두 장은 W0 에서 번들한 진짜 글꼴로 다시 찍었다. 성화의 서명·워터마크도 이 폴더가 적는다. 무엇을 보는 사진인지는 그 폴더의 `README.md` |
| 시안 계보 — atelier v5 인계본 (결정 11 전의 정본) | `docs/design/v5/` — `index.html` 한 파일에 화면 열과 기도 엔진, `art/` 에 성화 열여섯 장. `node docs/design/v5/build.mjs` 로 파일 하나짜리 조립본을 만든다. 경위와 슬롯 체계는 `docs/design/v5/README.md` |
| 기도 도메인 데이터 (81단계, 신비 4종, 54일 규칙, 기도문 임시 판본) | `spec/` — 프레임워크 무관 JSON·마크다운. PRD에 없는 것은 지어 넣지 않았다 |
| 옛 개발 계획 (마일스톤 M0~M5 — 결정 11 로 W0~W5 가 대체했다. 계정·스토어 체크리스트 §9 와 팀 §10 은 그대로 쓴다) | `docs/plan/development-plan.md` |
| 구현 대조표 — 요구사항 마흔둘 대비 구현 30 · 일부 6 · 없음 6 (2026-09-12) | `docs/plan/implementation-audit.md` |
| 엣지 기획안 — 우위가 어디서 나오나를 다시 물은 문서. §7 카드는 결정 11 이 흡수했다 | `docs/plan/edge-plan.md` |
| **W1 에서 찍은 화면 사진** | `docs/plan/w1-screens/` — 새 어법의 기도 화면 여덟 장. 너비 넷(320·375·390·430), 지금 자리가 십자가일 때와 고리 위의 알일 때, 그리고 둘째 슬라이스가 더한 둘(앱 안 글자 크기 `아주 크게` · 나가는 방법을 묻는 시트). 무엇을 보는 사진인지와 아직 담지 못한 것은 그 폴더의 `README.md` |
| **W3 에서 찍은 화면 사진** | `docs/plan/w3-screens/` — 열두 장이다. 슬라이스 A 의 다섯 장은 새 시안의 어법으로 다시 세운 여정 화면 넉 장(접힌 목록 · 펴진 줄을 390 과 320 두 너비에서 · 여정이 없는 화면)과 여정 완주 화면이고, 슬라이스 B 의 다섯 장은 성화 갤러리 석 장(지역 탭 · 모든 성화 탭 · 빈 즐겨찾기 탭)과 전체 화면 감상 두 장(껍데기가 보일 때와 사라졌을 때)이며, 슬라이스 C 의 두 장은 설정의 기록 내보내기·들여오기 두 줄과 들여오기 확인 시트다. 날짜 격자를 아홉 열로 통일한 판단을 두 너비의 사진으로 확인할 수 있고, 이 폴더가 대신하는 옛 사진 셋과 성화의 워터마크는 그 폴더의 `README.md` 가 적는다 |
| **W2 에서 찍은 화면 사진** (`gallery.png` · `settings.png` 은 **얼린 계보**) | `docs/plan/w2-screens/` — 새 시안의 어법으로 다시 세운 홈 넉 장(여정이 선 홈 · 빈 홈 · 이어서 바치는 홈 · `다시 바치기` 확인 시트)과 성화 갤러리 **자리 지킴이** 한 장. **`gallery.png` 은 2026-09-18 에 얼렸다** — W3 슬라이스 B 가 그 자리를 시안의 갤러리로 채웠고, 계속 찍으면 자리 지킴이의 기록이 덮이기 때문이다. 새 갤러리의 사진은 `docs/plan/w3-screens/` 에 있다. **`settings.png` 은 같은 날 같은 이유로 얼렸다** — W3 슬라이스 C 가 그 화면 아래쪽에 기록 내보내기·들여오기 두 줄을 세워 W2 의 기록이 덮였다. 새 설정 화면의 사진도 `docs/plan/w3-screens/` 에 있다. 홈 넉 장은 같은 날 **전체 화면으로 여는 단추**가 들어오며 다시 찍혔다. 시안과 달라 보이는 다섯 자리의 이유와 아직 옛 어법으로 남은 자리는 그 폴더의 `README.md` |
| W2 작업 지시서 (끝난 마일스톤) | `docs/plan/w2-work-order.md` — 홈·오늘의 신비·신비 해설·설정·지역 언어 다섯과 탭 바 넷, 성화 뽑기를 지역별 표로. 착수 전에 닫은 Q-51(강조색 글자값 다섯)과 통과 조건 여섯 |
| **W3 작업 지시서** (지금 여는 마일스톤) | `docs/plan/w3-work-order.md` — 여정 화면과 성화 갤러리·전체 화면 감상을 세우는 일. 착수 전에 재서 정한 넷(날짜 격자를 아홉 열로 · 여정을 만드는 자리를 한 곳으로 · 즐겨찾기는 새 저장 칸이고 고정은 있는 것을 쓴다 · Q-57 을 다시 잰다)과 통과 조건 여섯 |
| W1 작업 지시서 (끝난 마일스톤) | `docs/plan/w1-work-order.md` — 기도 화면을 새 시안의 어법으로 다시 세우는 일. 원형 고리 묵주·고리 드래그·성화 몰입 배경·글자 크기 넷·하루 완주 화면. 착수 전에 잰 사실 다섯과 통과 조건 여섯 |
| W0 작업 지시서 (끝난 마일스톤) | `docs/plan/w0-work-order.md` — 토큰·글꼴·언어 데이터·성화 표·설정 다섯을 앉히는 일. 착수 전에 잰 사실 넷과 통과 조건 다섯 |
| 옛 M0 작업 지시서 (v5 바닥 — 계보) | `docs/plan/m0-work-order.md` |
| M1·M2 에서 찍은 화면 사진 (기도 화면 두 장과 **홈 · 여정 상세 · 새 기도 · 여정 완주 넉 장**은 **얼린 계보**) | `docs/plan/m1-screens/`(기도 · 하루 완주) · `docs/plan/m2-screens/`(홈 · 새 기도 · 여정 상세 · 여정 완주 · 설정 · 초대 코드 · 시트 · 밤 벌) — v5 시안과 나란히 놓고 대조한다. **`home.png` 은 2026-09-18 에 얼렸다** — W2 가 홈을 새 어법으로 다시 세워 그 자리를 계속 찍으면 M2 의 기록이 덮이기 때문이다. 새 홈의 사진은 `docs/plan/w2-screens/` 에 있다. **같은 날 `journey.png` · `new.png` · `all-done.png` 도 같은 이유로 얼렸다** — W3 이 여정 상세와 새 기도를 여정 화면 하나로 합치고 여정 완주를 새 어법으로 옮겼다. 새 화면들의 사진은 `docs/plan/w3-screens/` 에 있다 |
| 알 쉰아홉을 다 그린 뒤의 화면 사진 (**얼린 계보**) | `docs/plan/rosary-full/` — 낮·밤 두 벌과 390×844·390×640 두 크기, 모두 네 장. 무엇을 보며 확인하는지는 그 폴더의 `README.md` (결정 6) |
| 기도 순서를 표준 도해에 맞춘 뒤의 화면 사진 (**얼린 계보** — 결정 12-2 로 묵주 기하가 바뀌었다) | `docs/plan/prayer-order/` — 시작 기도의 구원을 비는 기도, 제1단 첫 알(오른쪽), 제5단 마지막 알(왼쪽), 마침 기도의 성모찬송, 모두 넉 장. 무엇을 보는 사진인지는 그 폴더의 `README.md` (결정 7) |
| 묵주 그림을 사실적으로 다듬은 일의 전/후 사진 | `docs/plan/rosary-before-after/` — 같은 성화 위에서 고치기 전과 후를 나란히 놓았다. 무엇을 왜 바꿨는지는 그 폴더의 `README.md` |
| 늘어진 묵주 모양과 재질 넷을 그린 뒤의 화면 사진 (**얼린 계보** — 늘어진 고리는 이제 앱에 없다) | `docs/plan/rosary-materials/` — 낮 벌 넉 장(붉은 장미·나무·은·금), 밤 벌 한 장, 묵주 고르기 시트 한 장, 새 기도 화면의 묵주 줄 한 장, 모두 일곱. 무엇을 보며 확인하는지는 그 폴더의 `README.md` (결정 8·9) |
| 알을 다 채우고 진행을 빛으로 말하게 된 뒤의 화면 사진 (**얼린 계보**) | `docs/plan/rosary-lit/` — 낮 벌 넉 장(붉은 장미·나무·은·금), 밤 벌 한 장, 묵주 고르기 시트 한 장, 모두 여섯. 무엇을 보며 확인하는지와 낮 벌의 정직한 한계는 그 폴더의 `README.md` (결정 10) |
| **지금 알의 다섯 상태를 구현한 뒤의 화면 사진** | `docs/plan/bead-phases/` — 읽는 중 · 내 차례 · 멈춤 · 단 전환 · 소리 없이 진행, 모두 다섯 장. 동작 줄이기 상태로 찍어 움직임 없이도 갈리는지를 본다. 값의 출처와 한계는 그 폴더의 `README.md` (FR-15) |
| **글자를 200% 로 키운 기도 화면 사진** | `docs/plan/font-scale/` — 100% 한 장과 200% 두 장(첫 화면 · 사도신경). 줄 간격과 자간이 글자와 같은 비율로 커지는지를 본다. 플랫폼마다 React Native 가 이미 키우는 것과 우리가 키우는 것의 표, 기기 배율과 앱 안 글자 크기가 겹쳐 곱해질 때의 상한(52px), 실기기 미확인 한계는 그 폴더의 `README.md` (FR-28) |
| 음량 버튼(FR-38)을 지금 구조에서 붙일 수 있는지의 조사와 판정 | `docs/plan/volume-buttons.md` — Expo SDK 57 모듈과 React Native 에는 Android 음량 키를 앱에 알려 주는 통로가 없어 네이티브 부품이 필요하다는 결론, 부품 둘의 대가, M5 에서 공방장이 답할 질문 (Q-06a) |
| **성화의 서명·워터마크 판정표** (권리 일의 출발점) | `docs/plan/art-watermark-audit.md` — 열일곱 장을 **네 귀퉁이와 그림 전체**로 훑어 장마다 한 줄로 판정했다. 자국이 있는 것은 **여섯**(`01`·`02`·`04`·`11`·`12`·`13`)이고, 앱에 실리지 않은 `09` 까지 합쳐 일곱이다. 훑는 방법도 함께 적혀 있다 — 한 귀퉁이만 보는 훑기가 세 번 중 두 번 틀렸기 때문이다 (`decisions.md` Q-79) |
| **스토어 메타 초안** (한국어·영어) | `docs/plan/store-listing.md` — 이름·부제·짧은 설명·긴 설명·키워드·개인정보 라벨. **초안이고 사람의 승인 전에는 제출하지 않는다** |
| **개인정보 처리방침 초안** | `docs/plan/privacy-policy.md` — 계정도 서버도 없고 기록은 기기에만 남는다는 사실을 항목별로. **저장 층을 읽어 실제로 담기는 일곱 자리를 확인하고 썼다** — 코드와 문서가 어긋나면 그것은 거짓말이 되기 때문이다 |
| **심사 메모 초안** | `docs/plan/review-notes.md` — 애플·구글 심사자에게 줄 메모. 음량 버튼 미사용, 계정이 없어 애플 4.8·5.1.1 해당 없음, 심사에서 물어 올 것 넷의 답 |
| **스토어에 올릴 화면 사진** (**얼리지 않는 폴더** — 화면이 바뀌면 다시 찍는다) | `docs/plan/store-screenshots/` — 한국어·영어 다섯 장씩 열 장, 1290×2796. 검증용 대조 사진과 고르는 기준이 다르다(사람에게 보여 줄 것이다). 영어 벌에서 기도 화면을 뺀 이유는 그 폴더의 `README.md` (`decisions.md` Q-81) |
| **화면 사진 등록부** (얼린 사진과 살아 있는 사진을 가르는 정본) | `docs/plan/screens.json` — 사진 하나하나가 `live`(e2e 가 다시 찍는 것)인지 `frozen`(다시 찍으면 안 되는 기록)인지를 적는다. 얼린 줄마다 **어느 결정이 얼렸는지**와 **대신 볼 곳**이 함께 있다. `e2e/support/harness.ts` 의 감시가 얼린 자리에 찍으려는 시험을 그 자리에서 실패시키고, `npm run check:screens` 가 목록과 시험 코드의 어긋남을 잡는다. 왜 글이 아니라 목록이어야 했는지는 `decisions.md` Q-83 |
| **기도문 대조표** (공방장이 「가톨릭 기도서」와 맞대어 보는 자리) | `docs/plan/prayer-text-audit.md` — 앱이 말하는 아홉 기도문을 말하는 순서 그대로. 비어 있는 두 자리(사도신경 · 성모찬송 전문)와, Claude 가 그것을 채우지 않은 이유. 본문의 정본은 `spec/prayers.ko.json` 하나뿐이고 화면 문구 파일에는 기도문 이름만 있다 (`decisions.md` Q-84) |
| **성화 권리 문서** (로드맵 §7 의 "권리 문서 한 장") | `docs/plan/art-rights.md` — 그림을 직접 재서 나온 출처의 흔적 셋(가로 폭이 네 값에 몰림 · 작가 정보 없이 기기 색 프로필만 · Adobe Stock 미리보기 워터마크)과, 그래서 **자국 여섯 장이 아니라 열일곱 장 전부가 대상**이라는 판정. 길 셋의 비교와 공방장이 답할 두 가지. **§1-2 가 출처의 직접 증거를 담는다** — 인계 zip 의 `uploads/` 폴더에 원본 파일 이름(`다운로드 (13).jpg` 등)이 남아 있었고, 그 폴더는 저장소로 옮겨지지 않았다 (`decisions.md` Q-85 · Q-86) |
| **성화를 새로 만들어 달라는 Claude Design 지시문** | `docs/plan/art-remake-prompt.md` — 공방장이 Claude Design 에 그대로 붙여 넣는 글. 열여섯 장이 각각 무엇을 그리는지, 어느 지역에 쓰이는지, 앱에서 재서 나온 기술 조건 셋(세로 9:16 · 인물은 위쪽 절반 · 글자 없음), 하지 말 것, 완료 기준. **맨 앞이 "지금 쓰는 그림을 참고 이미지로 넣지 말라"로 시작한다** — 권리가 불분명한 그림을 보고 만들면 파생물이 되어 문제를 물려받기 때문이다 |
| **성화를 새로 만들 때 쓰는 프롬프트 열여섯 벌** | `docs/plan/art-prompt-pack.md` — 이미지 도구에 그대로 붙여 넣을 문장들. 앱을 읽어야 알 수 있는 조건 셋이 문장 안에 들어 있다 — 기도 화면의 덮개가 **아래 40% 를 가리므로 인물은 위쪽 절반에**, 초점은 세로 22~55% 에, 비율은 **세로 9:16 으로 통일**(지금의 정사각형 둘은 기도 화면에서 크게 잘린다). 모든 프롬프트 끝에 `no text, no signature, no watermark` 를 넣었다. **Claude 는 이 환경에서 그림을 만들 수 없다** — 이미지 생성 도구가 없고 바깥 인터넷도 막혀 있다 |
| 결정 로그·결정 큐·컨펌 필요 항목 | `decisions.md` — **일을 시작하기 전에 주제어로 한 번 검색한다** |
| 폐기 목록 (다시 쓰지 않기로 한 도구·자리) | `.claude/retired.json` — 훅이 차단한다 |
| 체계의 결함 기록 (창고 승격 후보) | `docs/lessons.md` |

## 이 저장소에서 일할 때 지킬 것

- **팀 공용 규칙은 `.claude/rules/*.md`가 세션 시작 시 자동으로 읽힌다.** 따로 `@`로 불러오지 않는다(창고 README 규정). 어투(communication), 에스컬레이션, 판돈, 원본 우선, 지난 결정, 교훈 백포트, DB 쓰기, 마이그레이션 파일명 — 여덟이다.
- **`.claude/` 아래 공용 자산은 여기서 고치지 않는다.** 원본은 창고(Template-repository)이고 동기화 봇이 내려보낸다. 이 저장소 소유는 `.claude/retired.json`, `.mcp.json`(Supabase 프로젝트가 생기면), `.github/workflows/`, `CLAUDE.md`, `decisions.md`, `docs/`, `spec/`, 그리고 앱 코드다.
- **PRD를 정정해야 하면 `docs/product/`에서 고치고 `decisions.md`에 사유를 남긴다.** 원문을 지우지 않고 개정 이력을 덧붙인다.
- **프레임워크·데이터 모델·인증 방향은 사람 관문이다**(에스컬레이션 §2 트리거 3). 결정 카드로 올리고, 답이 오기 전에는 그 결정에 의존하지 않는 일만 한다.
- **완료 어휘**: 작업완료(브랜치 푸시 + 검증 + 노출 증명) · 검토대기(PR) · 배포완료(main 머지 + 실환경 확인). 앱 화면이 생기면 "보는 법" 세 줄을 보고에 넣는다.
- **전체 e2e 를 돌린 뒤 화면 사진이 바뀌어 있으면 되돌리지 않는다.** 그 사진들은 **살아 있는 사진**이고, 바뀐 것은 화면이 실제로 바뀌었다는 뜻이므로 그대로 커밋한다. 정말로 얼린 사진이라면 애초에 찍히지 않고 시험이 실패한다 — `docs/plan/screens.json` 과 `e2e/support/harness.ts` 의 감시가 그것을 맡는다. 되돌리는 습관이 세 번 연속으로 옳은 갱신을 버렸다(`decisions.md` Q-83).
- **PR 자동 검사**가 `.github/workflows/`에 있다 — pr-gate-check(문서 갱신·배선), file-registry(등록부 없으면 no-op), branch-overlap(알림), asset-graph(자산 정합), integration-board(현황판 게시 — `.integration/` 설정을 이 프로젝트용으로 채워야 의미가 있다), sync-skills(창고 동기화). 창고 전용이던 readme-skills는 제거했다(decisions.md Q-12).

## 팀

`kimpm`(기획·결정·보고, 이 저장소의 리딩) · `kimdesigner`(구현된 화면을 v5와 대조 검수 — 결정 2로 역할이 바뀌었다) · `kimdeveloper`(구현 — 결정 3의 스택 위에서 M0부터) · `kimqa`(Playwright·qa-swarm 검증) · `doc-clarifier`(문서 정제). 큰 요청은 `/orchestrate`(KimLead)가 이들을 순서대로 지휘한다. 역할 상세는 `docs/plan/development-plan.md` §10.

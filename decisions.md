# 결정 로그 — My-Rosary-App

> 한 줄 요지: **이 저장소에서 "무엇을 하기로 했나"를 기록하는 유일한 장부다.** 규칙(`.claude/rules/`)은 세션 시작 시 자동으로 읽히지만 결정은 읽히지 않으므로, 일을 시작하기 전에 이 파일을 **주제어로** 한 번 검색한다(`grep -n -i "주제어" decisions.md`). 폐기한 도구·자리는 `.claude/retired.json`이 훅으로 막는다.

앞 단계(상품기획)의 결정은 atelier 저장소 `decisions.md`에 있다. 이 안건에 관한 것은 2026-08-26(첫 실사이클 안건 = MyRosary, D6 어디서 개발하나), 2026-08-28(DQ-01·02·04·05 판정), 2026-09-05(PRD 제3판 정정 아홉) 항목이다. 여기서는 개발 착수 이후의 결정만 적는다.

---

## 결정 0 (2026-09-05) — 이 저장소에서 개발한다. 정본은 여기로 옮기고, 양 플랫폼을 V1로 하며, 기획 컨펌 → 디자인 컨펌 → 개발 순서로 간다

**누가**: 공방장(geonwoo.kim). **어떻게**: 2026-09-05 발화 — "이게(프로토타입 zip) 프로토타입인데 이제부터 My-Rosary-App에서 실제로 앱을 개발하고 싶어, iOS랑 안드로이드 모두 올리게끔 만들고 싶어. kimpm 리딩해줘." 이어서 — "개발을 하기 전에 Atelier 레포에서 PRD 및 관련 문서를 확인해봐", "먼저 kimpm 통해 기획 먼저 하고 기획안 컨펌 받고, kimdesigner가 작업한 거 추가로 컨펌 받고 진행을 해줘."

이 발화가 정한 것 넷을 갈라 적는다.

| # | 정해진 것 | 무엇을 뒤집나 · 대가 |
|---|---|---|
| 0-1 | **MyRosary MVP는 이 저장소(`GeonwooKim-fitogether/My-Rosary-App`)에서 바로 개발한다.** atelier `decisions.md` 2026-08-26 D6의 세 갈래 중 **② "MyRosary repo에서 바로 개발하고 Foundry 절차는 다음 안건부터"** 를 택한 것이다 | atelier `plan.md` Phase 2(Foundry 인계 뒤 새 repo)의 보류 조건과 어긋나지 않는다 — 이관을 기다리지 않고 이 안건만 예외로 간다. Foundry 절차의 첫 적용은 다음 안건부터 |
| 0-2 | **제품 정본을 이 저장소 `docs/product/`로 이관하고 atelier 쪽은 freeze한다.** PRD 패키지 제3판 다섯 문서와 프로토타입 핸드오프를 커밋 `f39019b` 시점 사본으로 옮겼다 | 개발 중 PRD 정정은 여기서만 한다. atelier의 09 Handoff 문서는 이 저장소를 가리키는 링크 하나로 족하다 (atelier 쪽 작업은 이 저장소 범위 밖 — 참고 항목) |
| 0-3 | **Android를 V1 범위에 넣는다 — PRD §6 "플랫폼: V1은 iOS, Android는 V1.5" 및 §9 "Android · V1.5" 개정.** 공방장의 "iOS랑 안드로이드 모두"가 근거다 | 대가 셋. (1) 심사가 두 곳(App Store · Google Play)이고 각 스토어 계정·정책 준비가 둘. (2) 테스트 기기가 두 벌 — 공방장 실사용은 iOS, Android는 별도 검증자 또는 에뮬레이터. (3) FR-38 음량 버튼은 iOS에서 못 쓰므로(§11-3) 플랫폼별 동작 차이가 V1에 생긴다 — 기본값은 Q-06a. 프레임워크 카드의 "한 코드베이스로 양 플랫폼" 전제가 이 결정에서 나온다 |
| 0-4 | **진행 순서에 관문 둘을 둔다.** 관문 1 = 기획안(`docs/plan/development-plan.md`) 컨펌. 관문 2 = KimDesigner가 프로토타입을 바탕으로 앱 화면 시안을 확정·보완한 것의 컨펌. **개발(kimdeveloper)은 관문 2 뒤에만 시작한다** | 이번 턴의 산출물은 컨펌용 기획안이며 코드는 쓰지 않았다. 프레임워크 결정(아래)은 관문 1과 함께 답을 받는다 |

**PRD 본문은 아직 고치지 않았다.** 0-3의 §6·§9 개정과 아래 결정 큐의 기본값들은 관문 1 컨펌 뒤 한 번에 `docs/product/06-prd.md`에 반영한다(개정 이력 절을 PRD 끝에 덧붙이는 방식 — 원문을 지우지 않는다).

---

## 결정 대기 — 프레임워크·핵심 스택 (에스컬레이션 §2 트리거 3 · 사람 관문)

**상태: 상신됨 (2026-09-05). 답 대기.** 카드 본문은 `docs/plan/development-plan.md` §7에 있고, 최종 보고에도 같은 카드가 실렸다.

- **추천**: A안 — Expo(React Native, TypeScript) + EAS Build(클라우드 iOS·Android 빌드·제출) + Supabase(계정·데이터베이스·서버 함수).
- **무응답 시 기본 행동**: 관문 2(디자인 컨펌)가 끝나는 시점까지 답이 없으면 A안으로 M0(프로젝트 골격·CI·빈 앱 양쪽 빌드)에 착수한다. M0 산출물은 하루 안에 되돌릴 수 있다. M1(기도 루프 구현)부터는 확정된 답 없이 들어가지 않는다. 디자인 작업(관문 2)은 이 결정과 무관하므로 멈추지 않는다.
- **답이 오면**: 이 절을 "결정 1"로 바꿔 적고, 택한 안·사유·포기한 안을 남긴다.

---

## 결정 큐 — 기본값을 채택하고 진행하는 것 (에스컬레이션 §4)

관문 트리거(보안·라이브 DB·비가역 구조·범위 초과·비용·공개)에 해당하지 않는 판단은 기본값을 채택하고 여기 1행씩 남긴다. **"공방장 결정 필요"로 표시된 행만 컨펌 체크리스트에 올렸다** — 나머지는 이대로 진행하며, 다르게 보면 그 행만 고친다.

### 프로토타입 README "확정이 필요한 것 6개"와 정합 확인에서 나온 것

| # | 결정 내용 | 채택한 기본값 + 사유 | 되돌리기 | 검토 시점 |
|---|---|---|---|---|
| Q-01 | 시트 수 — PRD §5-1 "시트 여섯" vs §10 "시트 일곱"(S7 소개 시트 포함). 번호가 S2부터 시작 | **시트 일곱**(S7 포함 — 08 검증 참가자용 소개 시트는 검증 빌드에 필요하다). 번호는 재매기지 않고 S1 결번을 명시한다(06-d·08 테스트 케이스가 번호를 참조) | 쉬움 | 관문 1 |
| Q-02 | 초대 코드 입력의 자리 — 프로토타입은 별도 화면 "2. 초대 코드 입력"으로 그렸고, PRD(06-e 결함 12 처방)는 홈의 요소 하나(코드 입력)로 정의했다. §5-3 게이트 "화면 목록은 닫혔다" | **공방장 결정 필요.** 추천: PRD 따름 — 새 화면이 아니라 홈 위에 뜨는 **코드 입력 시트**로 두고, 프로토타입의 여섯 칸 입력·여정 미리보기 카드 UI는 그 시트 안에 살린다. 화면 목록을 다시 열지 않는다 | 쉬움 | **관문 1** |
| Q-03 | 조 여정의 진척 정의 — 다섯 중 셋만 바친 날을 며칠째로 세나 (FR-19·35·40 사이 공백). 조 기도 하루가 14단계인가 21단계(시작 기도 포함)인가 | 며칠째는 개인과 같이 **달력**으로 흐른다. 그날 칸은 "다섯 중 K"를 기록해 K=5면 채운 칸, 1~4면 반 채운 칸, 0이면 빈 칸으로 그린다. 남은 단은 자정까지 기다리고 자정 뒤 재배정하지 않는다(벌주지 않는 톤). 조 기도의 하루는 **내 단 14단계**만(시작 기도는 붙이지 않는다 — Rosario 구조와 PRD "한 단, 약 5분"에 맞춤) | 보통 — 날짜 기록에 K를 저장하면 표현만 바뀐다 | 08 검증 |
| Q-04 | 조 오프라인 첫날 — "배정을 못 받으면 어제와 같은 단"인데 첫날에는 어제가 없다 | 합류(코드 입력)는 연결이 있을 때만 가능하므로(06-e 결함 10 확장), **합류 순간 서버가 첫날 배정을 함께 내려주어 기기에 저장**한다. 그래도 배정이 없으면 제1단을 기본으로 바치고 연결되면 맞춘다 | 쉬움 | M3 |
| Q-05 | FR-45 로그아웃 — 오프라인에서 못 올린 오늘 자리가 사라진다. 예외 표에 처리가 없다 | 로그아웃 전에 **미동기화 기록을 먼저 올리고**, 연결이 없으면 로그아웃을 막지 않되 "아직 올리지 못한 오늘 자리가 있습니다. 나가면 이 기기에서 사라집니다"를 치자색 한 줄로 확인받은 뒤 진행한다(프로토타입 문구 "올리지 못한 기록은 먼저 보냅니다" 채택) | 쉬움 | M3 |
| Q-06a | FR-38 음량 버튼 — iOS는 심사 지침 2.5.9로 거절 가능성이 높다(§11-3). Android가 V1에 들어왔으므로(결정 0-3) Android에서 켤 것인가 | **V1 검증 빌드에서는 양 플랫폼 모두 음량 버튼을 쓰지 않는다.** 흔들기 + 이어폰 버튼만. Android 음량 버튼은 08 검증 결과를 보고 M5(정식 출시)에서 판단한다. 사유: 플랫폼별 동작 차이를 검증 기간에 만들지 않고, 네이티브 모듈 하나를 줄인다 | 쉬움 | 3차 판정 뒤 |
| Q-06b | 터치 영역 — PRD §6·06-b §7은 80px, 프로토타입은 iOS 44pt ≈ 88px를 제안 | **88px**(iOS 44pt·Android 48dp 기준을 모두 넘는 값) 채택. PRD §6과 06-b §7을 관문 1 뒤 갱신 | 쉬움 | 관문 1 |
| Q-07 | 새 기도(N)에서 낭송 방식을 인라인으로 노출할 것인가 — 프로토타입 README는 "차별점 #2이므로 인라인, 시트로 숨기지 말 것", PRD §3-5 2항·06-b §9 23번은 "고르는 일은 설정에서만" | **공방장 결정 필요.** 추천: PRD 따름 — N은 세 걸음(바람 · 형식+혼자/함께 · 시작일+판본)으로 두고 낭송 방식은 설정에서만 고른다. 차별점 2는 "첫 성모송에서 겪게 한다"(PRD §3-5 1항)로 이미 첫날 안에 드러난다 | 쉬움 | **관문 1** |
| Q-08 | 프로토타입이 새로 만든 "함께 바치기 배정" 화면과 그 안의 "다섯 단 모두 혼자 바치기" 단추 — PRD 화면 목록 밖. PRD는 조 배정을 홈 카드와 여정 상세(D)에 두고, §9는 다른 조 방식을 V1.5로 미뤘다 | **공방장 결정 필요.** 추천: PRD 따름 — 별도 화면과 단추를 두지 않는다. "오늘 내 몫은 제2단" 정보는 홈 카드 넷째 줄과 D의 조 영역에 이미 있다 | 쉬움 | **관문 1** |
| Q-09 | 치자-d 강조색 — 06-b는 `#8A6516`, 프로토타입은 본문 대비 4.5:1을 넘기기 위해 `#82600F`로 바꿨다(4.7:1) | **프로토타입 따름 `#82600F`.** 06-b §2-2 갱신 필요 | 쉬움 | 관문 2 |
| Q-10 | 전례 시기(FR-25) 계산 규칙 — PRD가 정하지 않았고 프로토타입 v4는 월(月)로 근사했다 | 부활 날짜 산출(공개 알고리즘)과 대림·사순·부활·성탄 시기 규칙을 코드로 넣고, 성모 축일·순교자 기념일은 연도 무관 고정 날짜 표로 둔다. 사제 자문이 필요한 항목이 아니다(공개된 전례력 규칙) | 쉬움 | M1 |
| Q-11 | 이 저장소의 진행 현황판 — atelier가 2026-08-27 폐기한 `progress-dashboard` 스킬이 창고 동기화로 이 저장소에도 내려와 있다 | **폐기 목록에 올린다**(`.claude/retired.json`). 현황판은 통합 현황판(integration-board) 하나. 결정 로그·결정 큐는 이 파일, 마일스톤은 기획안이 정본 | 쉬움 — retired.json 항목 삭제 | 필요 시 |
| Q-12 | 창고 전용 워크플로 `readme-skills.yml`과 `scripts/check-readme-skills.mjs` — README에 스킬 24개 표가 없으면 PR을 **차단**한다. 파일 자신이 "창고 전용, 하위 프로젝트에 배포하지 않는다"고 적고 있다 | **삭제.** 프로젝트 README는 이 제품을 설명해야 하고 창고 스킬 표를 유지할 이유가 없다. `asset-graph.yml`은 규칙 파일명이 README에 있으면 통과하므로 README에 팀 규칙 표를 두어 맞췄다 | 쉬움 — git에서 복원 | 필요 시 |

### PRD §11-1 "사람의 결정을 기다리는 것" — PRD의 기본값을 그대로 채택

| # | 결정 내용 | 채택한 기본값 + 사유 | 되돌리기 | 검토 시점 |
|---|---|---|---|---|
| D-1 | 배경 성화 확보 방법 (저작권 만료 고전 성화 / 사용 허락 현대 작품 / 새로 그림) | 검증 빌드까지는 **저작권 만료 고전 성화**(프로토타입 자산 4장은 목업 자산이므로 교체 슬롯으로 구현). 정식 출시 자산은 **비용 발생 가능(§2-5 트리거)** 이므로 M5 전에 별도 카드로 올린다 | 보통 — 이미지 슬롯 교체 | M5 전 |
| D-2 | 밤과 낮 중 기본값 | 두 벌을 동등 품질로 만들고(06-b §2-2) 기본값은 **기기 설정 따름**으로 두어 08 검증에서 정한다. 프로토타입은 낮 벌만 있으므로 밤 벌 시안은 관문 2의 KimDesigner 과제 | 쉬움 | 08 검증 |
| D-3 | 마침 기도 | 넣지 않는다(PRD 기본값 확정 2026-09-01) | 쉬움 | — |
| D-4 | 기도문 판본 목록과 각 판본의 사용 권한 | 검증 빌드는 **프로토타입 v4 수록문 한 판본**(`spec/prayers.ko.json`, 사도신경은 첫 문장만 — 지어 넣지 않았다)으로 진행하고, 판본 시트 S4는 자리만 잡는다. **공식 판본 전문 입력과 사용 권한 확인은 08 검증 시작 전에 마쳐야 한다**(검증 참가자가 실제로 바치므로). 권한 확인은 사람 몫 | 보통 — 데이터 파일 교체 | **M4 전 (사람 몫)** |

### §11-3 플랫폼 제약·확인 못 한 것

| 항목 | 처리 |
|---|---|
| 애플 로그인 의무(심사 4.8) | 확정 제약. FR-32대로 구글 + 애플 둘 다 |
| 54일 기도에서 하루를 건너뛰었을 때의 전통(사제 자문) | 개발과 무관하게 진행 가능. FR-35 기본값(강요하지 않음)으로 만들고, 자문 결과가 "반드시 처음부터"면 선택지를 눈에 띄게 두는 쪽으로 고친다. **사람 몫, 08 검증 전 권장** |
| 한국 앱 두 종의 실화면 · Marcello 자동 저장 여부 | 차별점 방어력 평가용. 개발에 영향 없음 |

---

## 컨펌 필요 — 사람이 답해야 진행되는 것 (관문 1)

기획안 `docs/plan/development-plan.md` §13 컨펌 체크리스트가 정본이다. 요약하면 다음 다섯이다. (1) 결정 0 네 항목 확인, (2) 프레임워크·스택 카드(추천 A안), (3) Q-02 초대 코드 입력 자리, (4) Q-07 낭송 방식의 N 인라인 여부, (5) Q-08 함께 바치기 배정 화면 유무. 그 밖의 결정 큐 행은 기본값으로 진행한다.

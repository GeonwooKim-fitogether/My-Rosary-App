# 결정 로그 — My-Rosary-App

> 한 줄 요지: **이 저장소에서 "무엇을 하기로 했나"를 기록하는 유일한 장부다.** 규칙(`.claude/rules/`)은 세션 시작 시 자동으로 읽히지만 결정은 읽히지 않으므로, 일을 시작하기 전에 이 파일을 **주제어로** 한 번 검색한다(`grep -n -i "주제어" decisions.md`). 폐기한 도구·자리는 `.claude/retired.json`이 훅으로 막는다.

앞 단계(상품기획)의 결정은 atelier 저장소 `decisions.md`에 있다. 이 안건에 관한 것은 2026-08-26(첫 실사이클 안건 = MyRosary, D6 어디서 개발하나), 2026-08-28(DQ-01·02·04·05 판정), 2026-09-05(PRD 제3판 정정 아홉) 항목이다. 여기서는 개발 착수 이후의 결정만 적는다.

---

## 결정 0 (2026-09-05) — 이 저장소에서 개발한다. 정본은 여기로 옮기고, 양 플랫폼을 V1로 하며, 기획 컨펌 → 디자인 컨펌 → 개발 순서로 간다

**누가**: 공방장(geonwoo.kim). **어떻게**: 2026-09-05 발화 — "이게(프로토타입 zip) 프로토타입인데 이제부터 My-Rosary-App에서 실제로 앱을 개발하고 싶어, iOS랑 안드로이드 모두 올리게끔 만들고 싶어. kimpm 리딩해줘." 이어서 — "개발을 하기 전에 Atelier 레포에서 PRD 및 관련 문서를 확인해봐", "먼저 kimpm 통해 기획 먼저 하고 기획안 컨펌 받고, kimdesigner가 작업한 거 추가로 컨펌 받고 진행을 해줘."

이 발화가 정한 것 넷을 갈라 적는다.

| # | 정해진 것 | 무엇을 뒤집나 · 대가 |
|---|---|---|
| 0-1 | **MyRosary MVP는 이 저장소(`GeonwooKim-fitogether/My-Rosary-App`)에서 바로 개발한다.** atelier `decisions.md` 2026-08-26 D6의 세 갈래 중 **② "MyRosary repo에서 바로 개발하고 Foundry 절차는 다음 안건부터"** 를 택한 것이다 | atelier `plan.md` Phase 2(Foundry 인계 뒤 새 repo)의 보류 조건과 어긋나지 않는다 — 이관을 기다리지 않고 이 안건만 예외로 간다. Foundry 절차의 첫 적용은 다음 안건부터 |
| 0-2 | **제품 정본을 이 저장소 `docs/product/`로 이관하고 atelier 쪽은 freeze한다.** PRD 패키지 제3판 다섯 문서와 프로토타입 핸드오프를 커밋 `f39019b` 시점 사본으로 옮겼다 | 개발 중 PRD 정정은 여기서만 한다. atelier의 09 Handoff 문서는 이 저장소를 가리키는 링크 하나로 족하다 (atelier 쪽 작업은 이 저장소 범위 밖 — 참고 항목) |
| 0-3 | **Android를 V1 범위에 넣는다 — PRD §6 "플랫폼: V1은 iOS, Android는 V1.5" 및 §9 "Android · V1.5" 개정.** 공방장의 "iOS랑 안드로이드 모두"가 근거다 | 대가 셋. (1) 심사가 두 곳(App Store · Google Play)이고 각 스토어 계정·정책 준비가 둘. (2) 테스트 기기가 두 벌 — 공방장 실사용은 iOS, Android는 별도 검증자 또는 에뮬레이터. (3) FR-38 음량 버튼은 iOS에서 못 쓰므로(§11-3) 플랫폼별 동작 차이가 V1에 생긴다 — 기본값은 Q-06a. 프레임워크 카드의 "한 코드베이스로 양 플랫폼" 전제가 이 결정에서 나온다 |
| 0-4 | **진행 순서에 관문 둘을 둔다.** 관문 1 = 기획안(`docs/plan/development-plan.md`) 컨펌. 관문 2 = KimDesigner가 프로토타입을 바탕으로 앱 화면 시안을 확정·보완한 것의 컨펌. **개발(kimdeveloper)은 관문 2 뒤에만 시작한다** | 이번 턴의 산출물은 컨펌용 기획안이며 코드는 쓰지 않았다. 프레임워크 결정(아래)은 관문 1과 함께 답을 받는다 |

**PRD 본문 반영은 2026-09-08 에 끝났다.** 0-3 의 §6·§9 개정과 결정 큐의 Q-06b·Q-09 는 `docs/product/06-prd.md` 와 `docs/product/06-design-system.md` 양쪽에 반영됐다. 반영 방식은 두 가지를 함께 썼다. 첫째, 두 문서 끝에 **개정 이력 표**를 두어 무엇이 무엇을 대체했는지 적었다. 둘째, 본문의 해당 줄에 그 표로 오는 **안내를 달았다** — 문서 끝까지 내려가지 않고 본문만 읽는 사람이 옛 값을 그대로 쓰는 일을 막기 위해서다. 원문은 지우지 않았고, 대체된 줄은 취소선으로 남겼다. 다만 디자인 시스템의 색 토큰 선언(`--chija-d`)만은 예외로 값 자체를 새 값으로 고치고 옛 값을 바로 아래 주석에 남겼다 — 토큰 블록은 그대로 복사해 코드에 넣는 자리라, 옛 값을 선언에 남겨 두면 대비 기준 미달인 색이 그대로 구현에 들어가기 때문이다.

---

## 결정 1 (2026-09-05) — 관문 1이 닫혔다. 화면 세 곳은 **프로토타입 방식**으로 간다

**누가**: 공방장. **어떻게**: 착수 브리프 제4장의 세 항목에 대해 **"모두 프로토타입 방식으로"**.

이 답은 Q-02·Q-07·Q-08 셋을 한꺼번에 정한 것이다. 세 항목 모두 KimPM의 추천(PRD 따름)과 **반대 방향**이며, 공방장의 답이 이긴다. 각 항목이 무엇을 뜻하는지와 그로 인해 함께 바뀌는 것을 아래에 적는다.

| # | 정해진 것 | 함께 바뀌는 것 |
|---|---|---|
| 1-1 (Q-02) | **초대 코드 입력은 별도 화면이다.** 프로토타입 "2. 초대 코드 입력" 그대로 — 머리(라벨·닫기), 제목 두 줄, 여섯 칸 코드 입력, 안내 문구, 들어갈 여정 미리보기 카드, 하단 `들어가기` 단추 | 화면 목록이 하나 늘어난다(아래 1-4). 진입점은 홈의 `초대 코드로 들어가기`. 프로토타입은 로그인 화면(L)에도 같은 진입점을 두었으나 **계정이 필수이므로(FR-32) 로그인 전 합류는 성립하지 않는다** — L의 진입점만은 빼고 홈에서만 연다. 이것은 프로토타입 방식을 뒤집는 것이 아니라 계정 필수와 모순되는 한 줄을 정리하는 것이다 |
| 1-2 (Q-07) | **새 기도(N)에서 낭송 방식을 인라인으로 고른다.** 프로토타입의 네 구역 구조 — ① 무엇을 위하여(지향 한 줄) ② 청원인가 감사인가 ③ 낭송(교대·전부 소리로·소리 없이 세 줄) ④ 묵주(현재 선택 한 줄, 고르기는 시트) | PRD §3-5 2항("고르는 일은 설정에서만")과 06-b §9 23번의 수용 기준이 **개정 대상**이 된다. 설정(E)의 낭송 방식·묵주 항목은 그대로 두되, 새 기도에서 고른 값이 **그 여정의 값**이 되고 설정 값은 새 여정의 기본값으로 남는다(FR-34의 판본 규칙과 같은 구조) |
| 1-3 (Q-08) | **"함께 바치기 배정" 화면을 만든다.** 프로토타입 "5. 함께 바치기 배정" 그대로 — 오늘 내 몫 제목, 맥락 두 줄, 다섯 줄 배정표(내 줄 강조), 합계 줄, `내 몫 바치기`, `다섯 단 모두 혼자 바치기` | 화면 목록이 또 하나 늘어난다(아래 1-4). **`다섯 단 모두 혼자 바치기`는 조 진척 계산에 새 경우를 만든다** — 한 사람이 다섯 단을 다 바친 날의 "다섯 중 K"(Q-03)를 어떻게 세는가. 기본값을 아래 Q-03에 덧붙였다 |
| 1-4 | **PRD의 "화면 목록은 닫혔다" 게이트(§5-3 · 06-e §5)를 다시 열어 화면 여덟을 열로 고친다.** 새 화면 둘 = `I 초대 코드 입력` · `G 함께 바치기 배정` | 06-e의 화면 지도·시나리오 검증(S-15 조 기도)과 06-d 화면 명세에 두 화면의 행이 새로 필요하다. 06-e 결함 12의 처방("코드 입력은 새 화면이 아니라 홈의 요소")은 이 결정으로 **대체된다** — 처방을 지우지 않고 "결정 1로 대체됨"을 덧붙인다 |

**왜 이 결정이 합리적인지 적어 둔다(반대 추천을 냈던 쪽의 정직한 기록).** 추천이 PRD 따름이었던 이유는 화면 수를 늘리지 않는 편이 퍼소나 B(시니어)에게 낫다는 것이었다. 공방장의 선택은 그 대신 **각 화면이 한 가지 일만 하게 하는 쪽**이다 — 여섯 칸 코드 입력을 시트에 넣으면 좁아지고, 조 배정을 홈 카드 한 줄로 줄이면 "오늘 내 몫이 몇 단인지"가 작아진다. PRD §5-1의 원칙("한 화면에서 할 일은 하나")과도 어긋나지 않는다. 낭송 방식 인라인은 프로토타입 README가 "차별점 #2를 시트로 숨기지 말 것"이라 적은 근거를 그대로 따른 것이다.

**이 시점에 남아 있던 것 둘.** (1) 결정 0 네 항목은 명시적 반대가 없어 그대로 확정한다(기획안 §13의 "답이 없으면 추천대로"). (2) 프레임워크·스택은 이때까지 답 대기였고, **2026-09-08에 결정 3으로 닫혔다**(A안).

**이 답의 범위.** 브리프 제4장이 물은 것은 세 항목이었고 "모두"를 그 셋으로 읽었다. 정합 확인표의 나머지 스무 행(계정 삭제 추가, 읽지 않기 자동 진행, 기도 화면 누락 요소 등)은 판정한 대로 간다. 만약 스물세 행 전부를 프로토타입 쪽으로 뒤집으라는 뜻이었다면 그렇게 말씀해 주시면 다시 판정한다 — 다만 그중 **계정 삭제(FR-45)는 애플 심사 필수라 뒤집을 수 없고**, 청원/감사를 사용자가 고르게 하는 것은 54일 기도의 정의(1~27일 청원, 28~54일 감사)와 충돌하므로 그 둘은 별도 확인이 필요하다.

---

## 결정 6 (2026-09-09) — 묵주에 알을 다 그리고, 단은 언제든 넘길 수 있게 한다

**누가**: 공방장. **어떻게**: 기도 화면을 보고 두 가지를 요청 — "어떤 상황에서든 단을 내가 마음대로 넘길 수가 있어야돼", "묵주가 모든 알이 다 반영되게 해줘".

**정해진 것 둘.** 첫째, 묵주 그림이 실제 묵주와 같은 **알 쉰아홉**을 다 그린다(늘어진 줄의 큰 알 하나와 작은 알 셋, 고리의 큰 알 다섯과 작은 알 쉰). 둘째, 기도 화면에 **앞 단·다음 단으로 옮기는 줄**을 두고, 시작 기도든 마지막 단이든 멈춘 상태든 언제나 눌리게 한다.

**무엇을 뒤집었나 — 두 문서를 각각 적는다.**

| 뒤집힌 것 | 원래 무엇이라고 적혀 있었나 | 이 결정이 정한 것 |
|---|---|---|
| PRD `docs/product/06-prd.md` §9 (V1 범위 밖) | "기도 중 배속과 건너뛰기 단추 · **만들지 않는다** · 기도는 탐색하는 것이 아니다" | 건너뛰기 단추를 **만든다.** 다만 만든 것은 배속도 알 단위 탐색도 아니라 **단(구간) 단위의 이동**이고, 앞/뒤 한 알을 되짚는 FR-38 은 그대로다. §9 의 그 행에는 원문을 지우지 않고 이 결정으로 대체됐다는 표시만 달았다 |
| v5 시안과 `src/prayer/rosaryState.ts` | "쉰아홉 알을 한 화면에 다 그리면 지금 어느 알인지가 보이지 않는다"는 판정으로 한 단(열 알)만 그렸다 | 쉰아홉을 다 그리되 **지금 알만 세 배 가까이 부풀려** 그 걱정을 푼다(아래) |

**시안이 걱정한 것을 어떻게 풀었나.** 알이 열에서 쉰아홉으로 늘면 알 하나가 작아져 "지금 어느 알인지"가 묻힌다. 디자인 시스템 §9-3 의 13번도 "알의 지름이 화면 폭의 8% 이상"을 요구한다. 두 요구를 함께 만족시키는 방법은 **모든 알을 같은 크기로 그리지 않는 것**이었다.

| | 반지름(그림 좌표) | 화면에서의 지름 | 화면 폭(390) 대비 |
|---|---|---|---|
| 작은 알 | 5.8 | 11.0px | 2.8% |
| 큰 알 | 7.8 | 14.8px | 3.8% |
| **지금 바치는 알** | **17** | **32.3px** | **8.3%** — 13번을 넘긴다 |

전체는 "얼마나 남았나"를 말하고, 부푼 알 하나는 "지금 어디인가"를 말한다. 부푼 알이 양옆 이웃을 한 알씩 덮지만, 빛무리가 이미 그만큼 번져 있어 잃는 것이 아니라 초점의 표시로 읽힌다. 실제로 낮·밤 두 벌과 390×844·390×640 두 크기로 렌더해 눈으로 확인했다(`docs/plan/rosary-full/`).

**함께 정한 작은 것 넷.**

1. **알에 머물지 않는 기도의 자리.** 성호경과 사도신경은 십자가가 빛나고, 시작 기도의 영광송은 중심 메달이 빛난다. 각 단의 영광송과 구원을 비는 기도는 **그 단의 열째 알에 머문다** — 실제로 그 두 기도는 열째 알을 쥔 채 바치고, 앞으로 나아간 척하지 않는 편이 정직하다.
2. **단을 넘기면 그 단의 첫 단계, 곧 신비 선포로 간다.** 뛰어든 사람에게 가장 먼저 필요한 것이 "지금 무엇을 묵상하는가"이기 때문이다.
3. **단 넘기는 줄의 자리는 성화 바로 아래, 기도문 위다.** 요구가 "어떤 상황에서든"이므로 스크롤하지 않고 언제나 보여야 하고, "지금 어디 있나"를 말하는 묵주 그림과 나란히 있는 편이 읽힌다. 치른 값은 기도문이 81 만큼 아래로 밀린 것이다 — 390×844 에서 `잠시 멈춤` 줄의 아래 절반이 첫 화면 밖으로 나간다.
4. **알을 눌러 그 단으로 뛰는 것은 넣지 않았다.** 작은 알의 화면 지름이 11px 이라 PRD §6 의 터치 영역 규정(80)에 한참 못 미치고, 기도 중의 잘못된 터치가 엉뚱한 단으로 데려가는 것이 두 단추보다 나쁘다.

**세는 법이 함께 바뀌었다.** 단을 건너뛰면 그 단의 성모송을 실제로 바치지 않은 것이므로, 하루 완주 화면의 "성모송 N번"이 큐를 세는 것으로는 참이 아니게 됐다. 이제 진행기가 **실제로 들른 단계만** 세고(`src/prayer/steps.ts` 의 `hailCountAmong`), 그 목록은 자리와 함께 기기에 저장돼 앱을 닫았다 열어도 이어진다. 제5단만 바치고 하루를 마치면 화면에 열 번이라고 적힌다.

**되돌리기.** 알을 다시 열 개로 줄이는 것은 `src/prayer/rosaryState.ts` 한 파일이고, 단 넘기는 줄을 빼는 것은 `app/pray.tsx` 의 블록 하나다. 세는 법만은 되돌리지 않는 편이 낫다 — 건너뛰기가 없어도 실제로 지나온 것을 세는 쪽이 참이기 때문이다.

**검수 대상.** 이 그림과 줄은 v5 시안에 없다. KimDesigner 가 렌더된 화면을 눈으로 검수한다(Q-14 가 시트·밤 벌에 쓴 방식과 같다).

---

## 결정 5 (2026-09-09) — 낮(한지)을 기본 화면으로 고정한다

**누가**: 공방장. **어떻게**: 폰에서 앱을 열었을 때 어두운 화면이 나오는 것을 보고 — "이게 밤이 아니라 낮으로 만들지 않았나?" 이어 "낮으로 고정해줘."

**정해진 것.** 앱을 처음 열면 **언제나 낮(한지) 벌**로 뜬다. 폰이 어둡게로 맞춰져 있어도 낮으로 연다. 밤(쪽빛)은 사라지지 않고 설정의 `낮과 밤`에서 고를 수 있으며, 고른 값은 기기에 남는다.

**무엇을 뒤집었나.** PRD D-2 는 "낮과 밤 두 벌을 같은 품질로 만들고 **기본값은 기기 설정 따름**으로 두어 08 검증에서 정한다"였다. 그 판단을 08 검증까지 기다리지 않고 지금 닫는다. D-2 의 원문은 지우지 않고 그대로 두며, 이 결정이 그것을 대체한다.

**왜 지금 닫았나.** 폰이 어둡게로 맞춰진 사람에게는 **시안이 그린 적 없는 화면이 이 앱의 첫인상이 된다.** 시안 정본(`docs/design/v5/`)은 낮 벌 열 화면만 담고 있고, 밤 벌은 v5 에 없어 구현자가 값에서 파생한 것이라 아직 KimDesigner 검수를 받지 않았다(Q-14 · Q-27). 검수받지 않은 화면이 남에게 보이는 첫 얼굴이 되는 것은 순서가 뒤바뀐 것이다.

**되돌리기.** 값 하나(`src/storage/settings.ts` 의 `DEFAULT_SETTINGS.theme`)와 그것을 지키는 시험 한 줄이다. 08 검증에서 밤이 낫다는 답이 나오면 그때 다시 연다.

---

## 결정 4 (2026-09-08) — 서버를 붙이기 전에 폰에서 먼저 써 본다

**누가**: 공방장. **어떻게**: 서버(M3)를 지금 열지, 폰에서 며칠 써 본 뒤에 열지를 물은 결정 카드에 — "폰에서 먼저 써 보는 걸로 가자."

**정해진 것.** 마일스톤의 **순서가 바뀐다.** 원래 계획은 M2 다음이 곧바로 M3(계정·서버·조)였으나, 그 사이에 **공방장의 실기기 실사용**을 넣는다. 서버 작업은 그 실사용에서 "손대지 않고 끝까지 가는가"에 대한 답을 얻은 뒤에 시작한다.

**왜 이 순서인가.** 이 제품의 성패는 잠자기 전 어두운 방에서 손 없이 하루가 끝까지 가는가에 달려 있고, 그 답은 폰에서만 나온다. 서버를 먼저 붙이면 아직 손에 맞는지 모르는 앱 위에 되돌리기 비싼 구조를 얹게 된다. 지금 구조는 기기 저장이 먼저이고 서버가 나중에 따라오는 방식이라, 뒤에 서버를 얹어도 화면과 기도 엔진을 고치지 않는다.

**이 결정이 곧바로 낳은 것 — 파일 하나짜리 웹 판.** 폰에서 써 보려면 원래는 개발자 계정 셋(Expo·Apple·Google)이 승인돼야 하고 거기에 하루 이틀이 걸린다. 그 대기를 없애기 위해 웹 빌드를 **파일 하나짜리 HTML**로 묶는 도구(`tools/pack/pack-single-html.mjs`)를 만들었다. 폰의 브라우저에서 주소 하나로 열린다.

**그 웹 판이 대신하지 못하는 것을 분명히 해 둔다.** 이것은 설치본이 아니라 그 앞에 두는 임시 통로다. 확인할 수 있는 것은 화면·흐름·자동 진행·한국어 낭송까지이고, 아래 셋은 여전히 설치본에서만 확인된다.

| 웹 판에서 확인되지 않는 것 | 왜 |
|---|---|
| 아이폰의 진동 | iOS 사파리는 웹 페이지에 진동을 허용하지 않는다. 안드로이드 크롬은 된다 |
| 이어폰 단추 | 앱이 "재생 중인 소리"를 잡아야 받을 수 있고, 그것은 설치본의 일이다 |
| 흔들기 | iOS 는 페이지에 움직임 센서를 주기 전에 사용자에게 한 번 묻는다. 그 허용 절차가 웹 판에는 없다 |

**남은 순서.** 웹 판으로 며칠 써 본다 → 계정 셋이 승인되면 설치본을 만들어 같은 것을 기기에서 다시 확인한다 → 그 뒤에 M3(계정·서버·조)를 연다.

---

## 결정 2 (2026-09-08) — 시안 정본을 atelier v5 인계본으로 바꾼다

**누가**: 공방장. **어떻게**: v5 통합 시제품을 건네며 — "이게 Atelier에서 시안을 어느 정도 확정한 상태인데 인수인계를 받아서 My-Rosary-App에서 인계를 받고 실제로 개발을 진행해보고자 해."

**정해진 것.** 화면의 모양·문구·값을 묻는 질문의 정본은 이제 `docs/design/v5/` 다. 이 저장소에서 앞서 만든 시안 파일들(`docs/design/screens-day.html` · `screens-night.html` · `prototype.html`)은 v5 의 조상이지만 **정본 자리에서 내려온다** — v5 의 값이 이기며, 두 쪽이 어긋나면 v5 를 따른다.

**인계하며 한 일.** atelier 가 건넨 자체완결본(그림이 박힌 3.1MB 한 파일)을 원본 형태로 되돌렸다. 성화 열여섯 장을 `art/` 로 꺼내고, 앱 코드는 54KB 한 파일로 남기고, 둘을 다시 한 파일로 합치는 조립기(`build.mjs`)를 붙였다. 두 형태 모두 브라우저에서 실제로 열어 확인했다.

**함께 정리한 것.** 인계본이 대체하는 앱 파일들을 저장소에서 지웠다. git 이력에는 남아 있으므로 되돌릴 수 있다.

**이 결정이 열어 주는 것.** 화면이 확정됐으므로 다음은 구현이다. 이 결정을 적을 당시에는 프레임워크·스택이 답 대기여서 M1 이후로 들어가지 못했는데, 같은 날 **결정 3**이 그 답을 받아 빗장을 풀었다.

**넘겨받은 자산 중 값진 것 하나를 기록해 둔다.** v5 는 성화 열여섯 장에 대해 **슬롯마다 초점 좌표를 따로** 갖는다(로그인 340px 띠 · 홈 96×124 썸네일 · 기도 148px 배경 · 완주 248px 띠). 같은 그림이라도 띠의 세로 비율이 달라지면 얼굴이 걸리는 자리가 달라지기 때문이고, 이것이 FR-23(묵주가 이목구비를 가리지 않는다)을 지키는 실제 방법이다. 어느 초점으로도 안 되는 그림은 그 슬롯에서 빠진다. 구현할 때 이 표를 값 그대로 옮긴다.

---

## 결정 3 (2026-09-08) — 스택은 A안이다. Expo(React Native, TypeScript) + EAS Build + Supabase

**누가**: 공방장. **어떻게**: 2026-09-08 발화 — "kimpm A안으로 가고 완료까지 진행해줘". 2026-09-05에 상신한 프레임워크 결정 카드(`docs/plan/development-plan.md` §7)에 대한 답이며, 그 카드의 추천과 같다.

**정해진 것.** 이 앱의 바닥은 아래 셋이다. 앞으로 모든 화면과 기능이 이 위에 놓인다.

| 층 | 무엇을 쓰나 | 이 층이 맡는 일 |
|---|---|---|
| 앱 | **Expo (React Native, TypeScript)** | 한 벌의 코드로 iOS와 Android 앱을 함께 만든다. 소리·진동·센서·화면 켜짐 같은 기기 기능은 Expo 공식 모듈로 붙인다 |
| 빌드·제출 | **EAS Build** (Expo가 운영하는 클라우드 빌드) | 맥이 없는 이 리눅스 환경에서 iOS 앱을 만들고 스토어에 올린다. 명령 한 줄이 클라우드의 맥에서 대신 돈다 |
| 서버 | **Supabase** | 계정 로그인(구글·애플), 데이터베이스(여정·기록·자리·조), 서버 함수(초대 코드 발급·검증, 날마다 배정) |

**왜 이 안인가 (사유).** 이 앱의 절반은 소리와 진동이다(PRD §4-6 — 낭송과 받는 사이, 진동 사전). 그 기기 기능을 웹뷰보다 안정적으로 다루면서, 동시에 팀이 이미 가진 검증 자산(Playwright · `qa-swarm`)을 웹 빌드로 계속 쓸 수 있는 안은 A안뿐이었다. 언어도 팀이 쓰던 TypeScript 그대로라 배우는 시간이 들지 않고, Supabase는 팀 규칙(`db-write-permission`)이 이미 전제하고 있어 안전장치가 그대로 걸린다.

**포기한 안 셋과 각각을 접은 이유.**

| 접은 안 | 무엇이 좋았나 | 왜 접었나 |
|---|---|---|
| **B. Flutter + Codemagic** | 기기 기능 패키지가 충분하고 화면이 빠르다 | 웹 빌드가 캔버스로 그려져 **Playwright와 `qa-swarm`이 화면 요소를 읽지 못한다.** 팀의 검증 방식이 통째로 무력해진다. 새 언어(Dart) 학습 1~2주도 얹힌다 |
| **C. 네이티브 둘 (SwiftUI + Kotlin)** | 각 플랫폼에서 가장 매끄럽다 | 코드가 두 벌이 되어 1인 팀에 과하고, 무엇보다 **맥이 없는 클라우드 리눅스에서 iOS 개발 자체가 성립하지 않는다** |
| **D. Capacitor (웹 앱을 네이티브 껍데기에 담기)** | 시안 HTML을 가장 많이 재사용한다. 무료 | 웹뷰에서 **한국어 음성 낭독과 이어폰 버튼, 백그라운드 소리가 불안정**하다. 이 앱에서 그 셋은 부가 기능이 아니라 차별점이다. 애플이 "웹사이트를 감싼 앱"(심사 지침 4.2)으로 볼 위험도 있다 |

**포기와 함께 감수하는 것 둘.** 첫째, EAS Build는 무료 구간(월 30회)을 넘기면 월 약 $19가 든다 — 비용이 실제로 발생하는 시점에 별도 카드로 올린다(에스컬레이션 §2 트리거 5). 둘째, 이어폰 리모트 버튼(FR-38)은 앱이 "재생 중"이어야 이벤트를 받으므로 오디오 세션 라이브러리가 하나 더 든다. 이 둘은 카드에 이미 적힌 위험이고, 답이 A안이므로 그대로 안고 간다.

**이 결정이 열어 주는 것 — M1 이후로 들어갈 수 있게 됐다.** 결정 2가 "무엇을 만드나"(v5 시안)를 닫았고, 이 결정이 "무엇으로 만드나"를 닫았다. 앞서 "프레임워크 답 없이는 M1 이후로 들어가지 않는다"고 걸어 둔 빗장이 이로써 풀린다. 남은 차단 요인은 사람 몫의 계정 셋(Expo · Apple Developer · Google Play Console)뿐이며, 그것은 M0의 클라우드 빌드와 M4의 배포를 막을 뿐 코드 작성을 막지 않는다.

**되돌리기.** 어렵다. 앱 코드를 다시 쓰지 않고는 바꿀 수 없다. 다만 `spec/`의 기도 도메인 데이터와 `docs/design/v5/`의 시안 값, Supabase 스키마는 어느 안으로 가도 그대로 살아남는 자산이라, 되돌릴 때 잃는 것은 화면 구현부에 한정된다.

---

## 결정 큐 — 기본값을 채택하고 진행하는 것 (에스컬레이션 §4)

관문 트리거(보안·라이브 DB·비가역 구조·범위 초과·비용·공개)에 해당하지 않는 판단은 기본값을 채택하고 여기 1행씩 남긴다. **"공방장 결정 필요"로 표시된 행만 컨펌 체크리스트에 올렸다** — 나머지는 이대로 진행하며, 다르게 보면 그 행만 고친다.

### 프로토타입 README "확정이 필요한 것 6개"와 정합 확인에서 나온 것

| # | 결정 내용 | 채택한 기본값 + 사유 | 되돌리기 | 검토 시점 |
|---|---|---|---|---|
| Q-01 | 시트 수 — PRD §5-1 "시트 여섯" vs §10 "시트 일곱"(S7 소개 시트 포함). 번호가 S2부터 시작 | **시트 일곱**(S7 포함 — 08 검증 참가자용 소개 시트는 검증 빌드에 필요하다). 번호는 재매기지 않고 S1 결번을 명시한다(06-d·08 테스트 케이스가 번호를 참조) | 쉬움 | 관문 1 |
| Q-02 | 초대 코드 입력의 자리 — 프로토타입은 별도 화면 "2. 초대 코드 입력"으로 그렸고, PRD(06-e 결함 12 처방)는 홈의 요소 하나(코드 입력)로 정의했다. §5-3 게이트 "화면 목록은 닫혔다" | **닫힘 — 결정 1-1 (프로토타입 따름).** 별도 화면 `I 초대 코드 입력`으로 만든다. 홈의 `초대 코드로 들어가기`에서만 열고, 로그인(L)의 진입점은 계정 필수와 모순이라 뺀다. ~~추천이었던 것: 홈 위 코드 입력 시트~~ | 쉬움 | 닫힘 |
| Q-03 | 조 여정의 진척 정의 — 다섯 중 셋만 바친 날을 며칠째로 세나 (FR-19·35·40 사이 공백). 조 기도 하루가 14단계인가 21단계(시작 기도 포함)인가 | 며칠째는 개인과 같이 **달력**으로 흐른다. 그날 칸은 "다섯 중 K"를 기록해 K=5면 채운 칸, 1~4면 반 채운 칸, 0이면 빈 칸으로 그린다. 남은 단은 자정까지 기다리고 자정 뒤 재배정하지 않는다(벌주지 않는 톤). 조 기도의 하루는 **내 단 14단계**만(시작 기도는 붙이지 않는다 — Rosario 구조와 PRD "한 단, 약 5분"에 맞춤). **결정 1-3으로 덧붙인 경우**: 한 사람이 `다섯 단 모두 혼자 바치기`로 다섯 단을 다 바친 날은 그 사람이 다섯 단을 채운 것으로 보아 K=5(채운 칸)로 센다. 다른 조원이 그날 자기 단을 따로 바치면 그 기록은 개인 기록으로 남기되 K는 5를 넘지 않는다. 혼자 다 바칠 때의 하루는 예외적으로 **77단계 전부**다(시작 기도 포함 — 개인 기도와 같은 구조) | 보통 — 날짜 기록에 K를 저장하면 표현만 바뀐다 | 08 검증 |
| Q-04 | 조 오프라인 첫날 — "배정을 못 받으면 어제와 같은 단"인데 첫날에는 어제가 없다 | 합류(코드 입력)는 연결이 있을 때만 가능하므로(06-e 결함 10 확장), **합류 순간 서버가 첫날 배정을 함께 내려주어 기기에 저장**한다. 그래도 배정이 없으면 제1단을 기본으로 바치고 연결되면 맞춘다 | 쉬움 | M3 |
| Q-05 | FR-45 로그아웃 — 오프라인에서 못 올린 오늘 자리가 사라진다. 예외 표에 처리가 없다 | 로그아웃 전에 **미동기화 기록을 먼저 올리고**, 연결이 없으면 로그아웃을 막지 않되 "아직 올리지 못한 오늘 자리가 있습니다. 나가면 이 기기에서 사라집니다"를 치자색 한 줄로 확인받은 뒤 진행한다(프로토타입 문구 "올리지 못한 기록은 먼저 보냅니다" 채택) | 쉬움 | M3 |
| Q-06a | FR-38 음량 버튼 — iOS는 심사 지침 2.5.9로 거절 가능성이 높다(§11-3). Android가 V1에 들어왔으므로(결정 0-3) Android에서 켤 것인가 | **V1 검증 빌드에서는 양 플랫폼 모두 음량 버튼을 쓰지 않는다.** 흔들기 + 이어폰 버튼만. Android 음량 버튼은 08 검증 결과를 보고 M5(정식 출시)에서 판단한다. 사유: 플랫폼별 동작 차이를 검증 기간에 만들지 않고, 네이티브 모듈 하나를 줄인다 | 쉬움 | 3차 판정 뒤 |
| Q-06b | 터치 영역 — PRD §6·06-b §7은 80px, 프로토타입은 iOS 44pt ≈ 88px를 제안 | ~~**88px** 채택~~ **닫힘 — Q-16 이 이 항목을 대체했다(2026-09-08).** 88 이라는 숫자가 단위 환산 착오에서 나온 것이 M0 구현 중에 드러나, 80px 을 유지하는 것으로 정리됐다 | 쉬움 | 닫힘 |
| Q-07 | 새 기도(N)에서 낭송 방식을 인라인으로 노출할 것인가 — 프로토타입 README는 "차별점 #2이므로 인라인, 시트로 숨기지 말 것", PRD §3-5 2항·06-b §9 23번은 "고르는 일은 설정에서만" | **닫힘 — 결정 1-2 (프로토타입 따름).** N에서 낭송 방식을 인라인으로 고른다. 프로토타입의 네 구역(지향 · 청원/감사 · 낭송 세 줄 · 묵주 한 줄) 구조를 쓴다. PRD §3-5 2항과 06-b §9 23번이 개정 대상이 된다. ~~추천이었던 것: 설정에서만~~ | 쉬움 | 닫힘 |
| Q-08 | 프로토타입이 새로 만든 "함께 바치기 배정" 화면과 그 안의 "다섯 단 모두 혼자 바치기" 단추 — PRD 화면 목록 밖. PRD는 조 배정을 홈 카드와 여정 상세(D)에 두고, §9는 다른 조 방식을 V1.5로 미뤘다 | **닫힘 — 결정 1-3 (프로토타입 따름).** 별도 화면 `G 함께 바치기 배정`을 만들고 `다섯 단 모두 혼자 바치기` 단추도 둔다. 조 진척 계산의 새 경우는 Q-03에 덧붙였다. ~~추천이었던 것: 만들지 않음~~ | 쉬움 | 닫힘 |
| Q-09 | 치자-d 강조색 — 06-b는 `#8A6516`, 프로토타입은 본문 대비 4.5:1을 넘기기 위해 `#82600F`로 바꿨다(4.7:1) | **프로토타입 따름 `#82600F`.** 06-b §2-2 갱신 필요 | 쉬움 | 관문 2 |
| Q-10 | 전례 시기(FR-25) 계산 규칙 — PRD가 정하지 않았고 프로토타입 v4는 월(月)로 근사했다 | 부활 날짜 산출(공개 알고리즘)과 대림·사순·부활·성탄 시기 규칙을 코드로 넣고, 성모 축일·순교자 기념일은 연도 무관 고정 날짜 표로 둔다. 사제 자문이 필요한 항목이 아니다(공개된 전례력 규칙) | 쉬움 | M1 |
| Q-11 | 이 저장소의 진행 현황판 — atelier가 2026-08-27 폐기한 `progress-dashboard` 스킬이 창고 동기화로 이 저장소에도 내려와 있다 | **폐기 목록에 올린다**(`.claude/retired.json`). 현황판은 통합 현황판(integration-board) 하나. 결정 로그·결정 큐는 이 파일, 마일스톤은 기획안이 정본 | 쉬움 — retired.json 항목 삭제 | 필요 시 |
| Q-12 | 창고 전용 워크플로 `readme-skills.yml`과 `scripts/check-readme-skills.mjs` — README에 스킬 24개 표가 없으면 PR을 **차단**한다. 파일 자신이 "창고 전용, 하위 프로젝트에 배포하지 않는다"고 적고 있다 | **삭제.** 프로젝트 README는 이 제품을 설명해야 하고 창고 스킬 표를 유지할 이유가 없다. `asset-graph.yml`은 규칙 파일명이 README에 있으면 통과하므로 README에 팀 규칙 표를 두어 맞췄다 | 쉬움 — git에서 복원 | 필요 시 |
| Q-13 | 로그인 화면(L)의 `초대 코드로 들어가기` 진입점 — 결정 1-1은 "계정이 필수라 로그인 전 합류는 성립하지 않으므로 L의 진입점은 뺀다"고 정했는데, **v5 시안 `index.html`의 로그인 화면에는 그 줄(`go-invite1`)이 아직 남아 있다.** 시안 정본(결정 2)과 결정 1-1이 한 줄에서 어긋난다 | **결정 1-1이 이긴다 — 구현에서 그 줄을 뺀다.** 사유: 결정 2가 v5에 준 권한은 "화면의 모양·문구·값"의 정본이지 이미 내린 동작 결정을 뒤집는 권한이 아니다. 나머지 값(간격·색·서체)은 v5 그대로 두고, 그 한 줄만 지운 채 아래 여백을 그만큼 늘린다 | 쉬움 — 줄 하나 되살리기 | M0 (로그인 화면 구현) |
| Q-14 | **시트 일곱(S2~S7과 계정 삭제 확인)과 밤 벌이 v5에 없다.** v5는 낮 벌 화면 열만 담고 있고, PRD·06-d가 요구하는 바텀 시트와 06-b §2-2가 요구하는 밤 벌 쪽빛 테마는 그리지 않았다. 그런데 관문 2가 결정 2로 닫히면서 그것을 그릴 사람(KimDesigner의 §8 과제 D-A·D-I)도 함께 사라졌다 | **범위는 줄이지 않고, 만드는 순서를 뒤집는다.** 시트와 밤 벌은 v5가 확립한 낮 벌 값(색 토큰·서체 계단·괘선 구조·80px 줄 높이)에서 **구현자가 파생해 코드로 먼저 만들고**, KimDesigner는 시안을 새로 그리는 대신 **렌더된 화면을 눈으로 검수**한다. 사유: v5가 값 체계를 이미 닫아 두어 파생이 창작이 아니라 적용이고, 시안 HTML을 한 벌 더 그리는 것은 같은 값을 두 번 적는 일이다 | 보통 — 검수에서 어긋나면 그 화면만 다시 | M2 (시트·밤 벌이 실제로 필요해지는 시점) |
| Q-15 | Expo 프로젝트를 저장소 어디에 앉히나 — 루트에는 이미 `docs/` · `spec/` · `.claude/` · `.github/`가 있다 | **루트에 앉힌다.** `package.json` · `app.json` · `eas.json`은 루트, 앱 소스는 `app/`(화면 라우팅) · `src/`(도메인·토큰·유틸) · `assets/`(폰트·성화). 사유: Expo 도구 사슬(EAS Build·`expo` CLI)이 저장소 루트를 프로젝트 루트로 가정할 때 설정이 가장 적고, 하위 폴더에 넣으면 CI와 EAS 양쪽에 작업 폴더 설정을 따로 얹어야 한다. `docs/`·`spec/`은 앱이 import 하거나 참조하는 대상이라 루트에 함께 있는 편이 경로가 짧다 | 보통 — 폴더 이동 + 설정 경로 수정 | M0 |
| Q-16 | **터치 영역이 80px 인가 88px 인가 — 문서와 코드가 어긋나 있었다.** Q-06b 가 88px 를 채택해 디자인 시스템 문서에 반영됐는데, v5 시안 37 곳과 M0 구현 코드는 80 이다 | **80px 을 유지한다.** 사유는 단위다. 88 이라는 숫자는 프로토타입 README 가 iOS 의 44pt 를 2배 화면의 *기기 픽셀* 로 환산해 얻은 값(`iOS 44pt ≈ 88px`)인데, 코드·CSS·시안이 쓰는 단위는 기기 픽셀이 아니라 pt(iOS)·dp(Android) 다. 그 단위에서 iOS 권고는 44, Android 권고는 48 이므로 **80 은 이미 둘 다 넉넉히 넘는다.** 즉 Q-06b 가 내세운 근거("두 플랫폼 기준을 모두 넘는 값")를 80 이 이미 만족하며, 88 로 올리면 v5 의 화면 높이 37 곳이 함께 움직여 시안 정본과 구현이 갈라진다. 반영 위치: PRD §6, 06-b §7·§9-4, 06-d 개정 이력, 개발 계획 §7 표 19 행 | 쉬움 — 토큰 한 값(`metrics.touchTargetHeight`)과 문서 네 곳 | 이번 보고에서 검토 |
| Q-17 | **로그인 단추가 어디로 들어가나 — 홈 화면은 M2 에 있다.** M1 에서 만든 화면은 기도와 하루 완주 둘인데, v5 의 배선은 로그인에서 홈으로 가고 홈의 여정 카드에서 기도로 들어간다. 그 가운데 화면이 아직 없다 | **M1 동안만 로그인 단추가 기도 화면으로 곧바로 들어간다.** 사유: 사용자가 첫 화면에서 눌러 닿을 수 없는 기능은 만들어지지 않은 것과 같다(communication 규칙 7-2). v5 에서도 `Google로 계속하기` 는 계정을 확인하지 않고 앱 안으로 들어가므로, 바뀌는 것은 목적지 하나뿐이다. 실제 인증은 M3 | 쉬움 — `app/index.tsx` 의 목적지 한 줄과 `src/navigation/leaveToHome.ts` | M2 (홈이 서는 시점) |
| Q-18 | **전례색의 낮 벌 값이 하나뿐이다.** 06-design-system §2-3 은 전례색 넷을 밤 팔레트 값으로만 적었고, v5 는 그중 자색 하나(`#63507F`)만 기도 화면 구간 라벨에 썼다. Q-10 이 시기 계산을 M1 으로 정했으므로 나머지 셋의 낮 값이 필요해졌다 | **규칙 하나로 파생했다** — 밤 값의 색상과 채도를 유지하고 낮 바탕(`#EDE7D8`) 위에서 대비 4.5:1 을 넘길 때까지 명도만 낮춘다. 결과는 자색 `#63507F`(v5 그대로) · 녹색 `#5F6850` · 홍색 `#9E4A57`(밤 값이 이미 4.77:1) · **백색은 먹빛 `#1F2530`**(밝은 한지 위에서는 어떤 명도로도 백색이 백색으로 읽히지 않으므로 빛깔을 더하지 않는다). 함께 정한 것: 기도 화면의 구간 라벨은 고정 자색이 아니라 **그날 시기의 색**으로 칠한다 — 그 자리가 FR-25 가 말하는 슬롯이기 때문이다 | 쉬움 — `src/theme/tokens.ts` 의 `seasonColors` 네 값 | **KimDesigner 검수** (M2 의 밤 벌 작업과 함께) |
| Q-19 | **하루 완주 화면의 "성모송" 수 — v5 는 50 번, 실제 77단계에는 53 번이 들어 있다.** 시작 기도의 성모송 셋을 세는가 마는가의 차이다 | **실제로 센 값(53)을 보인다.** 사유: 완주 화면의 숫자는 방금 바친 기도를 확인해 주는 것이라 실제와 달라지면 그 화면이 하는 일이 무너진다. 세는 방법은 Q-03 이 조 기도에 대해 이미 정한 것과 같다(그 단의 성모송을 센다 — 조 기도 하루는 10) | 쉬움 — `hailCount` 를 부르는 자리 | 08 검증 |
| Q-20 | **하루 완주 화면의 머리글이 어느 날을 가리키나.** v5 의 `renderDayDone` 은 리본에서 `오늘` 이 다음 칸으로 옮겨간 뒤에 날짜를 읽어, 23일째를 바치고 나면 `스물네 번째 날` 이라고 적는다 | **방금 바친 날(스물세 번째 날)을 적는다.** 사유: 하루 완주 화면이 확인해 주는 것은 내일이 아니라 오늘이다. 리본은 v5 그대로 다음 칸이 오늘로 넘어간 모습을 보인다 | 쉬움 — 넘겨주는 값 하나 | 08 검증 |
| Q-21 | **손 없이 조작의 기본값 — 설정 화면(E)이 M2 라서 켜고 끌 자리가 아직 없다.** `spec/journey-rules.md` §6 은 "설정에서 켠 경우에만"이라고 적었다 | **M1 동안은 켜 둔다.** 사유: 08 검증의 중심 질문이 "화면을 보지 않고 손을 쓰지 않고도 다섯 단을 끝까지"(DQ-05)이므로, 그 입력이 꺼져 있으면 검증할 것이 없다. M2 에서 설정 화면이 서면 그 값이 이 기본값을 대신한다 | 쉬움 — 갈고리 인자 하나 | M2 |
| Q-22 | **구간 이름에 쓰는 기도문 이름이 v5 와 `spec/` 에서 다르다.** v5 는 `제1단 · 묵상` · `제1단 · 구원송` 으로 줄여 적었고, `spec/prayers.ko.json` 의 이름은 `신비 선포` · `구원을 비는 기도` 다 | **형식은 v5(`구간 · 기도문 이름`), 이름은 `spec/`.** 사유: 화면의 형식은 시안이 정본이고 기도문의 이름은 도메인 데이터가 정본이다. 이 갈래로 두 곳이 어긋날 때마다 판정을 다시 하지 않는다 | 쉬움 — `src/prayer/steps.ts` 의 `headFor` | 08 검증 |
| Q-23 | **이어폰 단추가 실기기에서는 아직 동작하지 않는다.** 웹에서는 브라우저의 미디어 세션에 손잡이를 걸어 두었고 e2e 로 확인했지만, iOS·Android 에서 이어폰 단추를 받으려면 앱이 "재생 중"인 오디오 세션을 잡고 있어야 한다(개발 계획 §11 이 예고한 위험) | **M1 에서는 웹 배선까지만 하고, 오디오 세션은 뒤로 미룬다.** 사유: 무음 트랙 위에 낭송을 얹는 작업은 그 자체로 하나의 작업 덩어리이고, 흔들기만으로도 손 없이 조작의 중심 질문은 검증할 수 있다. 실기기 확인 때 이어폰 단추가 필요하다는 회신이 오면 그때 붙인다 | 보통 — 오디오 세션 라이브러리 하나 | 공방장 실기기 회신 뒤 |
| Q-24 | **v5 시안 안에서 여정의 시작일이 두 값으로 어긋나 있다.** 여정 상세 화면에는 `7월 14일 시작` 이라는 글자가 박혀 있는데, 날짜를 계산하는 `dateKo()` 는 23일째를 9월 5일로 놓는다(그러면 시작일은 8월 14일이다) | **계산에 쓰이던 쪽(23일째 = 9월 5일)을 따랐다.** 사유: 글자는 예시이고 계산은 화면 여러 곳을 함께 움직인다. 여정 상세는 M2 의 화면이므로 그때 다시 확인한다 | 쉬움 — 시드 값 하나 | M2 (여정 상세) |
| Q-25 | **새 기도(N)에 형식을 고르는 자리가 v5 에 없다.** M2 의 범위에 여정 형식 셋이 들어 있고(개발 계획 §6-3) FR-33 이 형식 선택을 새 기도의 일로 정했는데, v5 의 새 기도는 54일 기도 하나를 전제로 그려졌다 | **구역 하나를 파생해 더했다.** 모양은 v5 가 바로 아래 구역(낭송)에 쓴 줄 세 개를 그대로 쓰고, 문구는 06-screen-spec 화면 N 의 문구 표에 확정돼 있는 것(`27일 청원, 27일 감사. 하루에 다섯 단.` 등)을 옮겼다. Q-14 가 시트·밤 벌에 정한 방식(v5 값에서 파생 → KimDesigner 렌더 검수)을 같은 성격의 빈자리에 한 번 더 적용한 것이다. 고르는 자리가 없으면 형식 셋은 코드에만 있고 사용자에게는 없는 것과 같다 | 쉬움 — 구역 하나 제거 | **KimDesigner 검수** |
| Q-26 | **설정(E)의 항목이 v5 와 PRD 사이에서 갈린다.** v5 의 설정은 프로토타입 시절 목록이라 PRD 가 요구하는 다섯(받는 사이 · 손 없이 조작 · 묵주 · 계정 삭제 · 소개)이 없고, PRD 가 빼기로 한 둘(글자 크기 · 기도 시각 알림)이 있다 | **개발 계획 §3 의 14 행 판정("PRD 따름")을 그대로 집행했다.** 줄의 모양(높이 80 · 위 괘선 · 14px 이름 · 13px 값)은 v5 그대로 두고 항목만 PRD 로 맞췄다. 계정 삭제는 애플 심사 5.1.1 이 요구하는 경로라(FR-45) 빼면 출시가 막힌다. **계정 정보 줄(FR-41)만은 넣지 않았다** — 표시 이름도 제공자도 M3 에 생기므로 없는 것을 지어 적지 않았다 | 쉬움 — 줄 단위 | **KimDesigner 검수** |
| Q-27 | **밤 벌 전례색 넷 중 둘이 본문 대비 기준에 미달한다.** 06-design-system §2-3 의 밤 값을 그대로 옮기고 대비를 재 보니 자색 `#7A6699` 가 3.62:1, 홍색 `#9E4A57` 이 3.09:1 이었다(쪽빛 바탕 위). 문서가 상정한 쓰임은 면과 선인데 v5 는 이 슬롯을 10.5px 글자에 썼다 | **Q-18 이 낮 벌에 쓴 규칙을 방향만 뒤집어 적용했다** — 색상·채도를 두고 명도만 4.5:1 을 넘길 때까지 올린다. 자색 `#8C7AA6`(4.71:1) · 홍색 `#BA6B77`(4.72:1). 녹색과 백색은 문서 값 그대로다. 대비는 눈이 아니라 `src/theme/tokens.test.ts` 가 계산해 지킨다 | 쉬움 — 두 값 | **KimDesigner 검수** · 06-b §2-3 에 적을지는 사람이 정한다 |
| Q-28 | **손 없이 조작의 기본값 — PRD 는 꺼짐, 검증은 켜짐을 요구한다.** Q-21 이 M1 에 대해 "켜 둔다"로 정하며 M2 에서 설정 화면이 서면 그 값이 대신한다고 했다 | **검증 빌드의 기본값을 켜짐으로 둔다.** 사유: 08 검증의 중심 질문이 "화면을 보지 않고 손을 쓰지 않고도 다섯 단을 끝까지"(DQ-05)인데, 꺼진 채로 배포되면 참가자 대부분이 그 입력을 한 번도 만나지 않는다. 끄는 자리는 설정에 있고, 정식 출시(M5)에서 PRD 기본값으로 되돌릴지 다시 본다 | 쉬움 — 기본값 한 줄 | 08 검증 |
| Q-29 | **여정 완주 화면의 v5 문구 한 마디가 이 앱의 규칙과 어긋난다.** v5 는 `성모송 2,700번 · 못 바친 날은 뒤로 밀려 채워졌습니다` 라고 적었는데, FR-35 는 못 바친 날을 뒤로 밀지 않고 빈 칸으로 남긴다 | **그 마디를 빼고 성모송 수만 남겼다.** 없는 일을 말하는 문장이라 그대로 옮길 수 없었고, 대신할 문구를 지어내지도 않았다. 완주 화면이 못 바친 날을 어떻게 말할지는 문구가 필요하면 사람이 정한다 | 쉬움 — 한 줄 | **KimDesigner 검수** |
| Q-30 | **여정 상세의 `청원에서 감사로` 가 v5 는 27일째, FR-35 는 28일째다** (1~27일 청원) | **28일째로 적는다.** 이 줄이 말하는 것은 화면의 모양이 아니라 기도의 규칙이므로 규칙 쪽을 따랐다 | 쉬움 | 08 검증 |
| Q-31 | **v5 새 기도의 구역 ②(청원/감사)가 무엇을 바꾸는지 시안에 없다.** 상자에 적힌 것은 `27일 + 27일` 과 `쉰네 날 내내` 뿐이다 | **그 두 문구를 그대로 규칙으로 읽었다.** 청원으로 시작하면 1~27일 청원 · 28~54일 감사(FR-35 그대로), 감사로 시작하면 쉰네 날 내내 감사다. 여정 상세와 완주 화면의 문구도 이 갈래를 따른다 | 쉬움 | 08 검증 |
| Q-32 | **본보기 여정을 어떻게 세우나.** 처음 여는 앱의 정상 상태는 빈 홈인데(06-screen-spec 화면 A), 빈 홈으로는 카드·리본·격자를 시안과 대조할 수 없다 | **웹 주소의 손잡이 `?demo=1` 로 세운다**(`?demo=reset` 은 버리고 다시). v5 에도 같은 성격의 진단용 손잡이가 있었다(`?art=01,02` 로 성화 뽑기 고정). 실제 사용자는 이 주소를 지나가지 않고, 앱 안에는 이 여정을 만드는 단추가 없다 | 쉬움 — 손잡이 제거 | 필요 시 |
| Q-33 | **로그아웃과 계정 삭제를 눌렀을 때 실제로 무엇이 일어나나.** 두 시트는 M2 의 범위(화면과 확인 흐름)인데 계정 자체가 M3 다 | **확인 시트를 거치면 로그인 화면으로 돌아가는 데까지만 한다.** 서버에서 지울 것도, 이 기기에서 나갈 계정도 아직 없기 때문이다. **기기에 저장된 여정은 지우지 않는다** — 06-e 결함 11 이 요구하는 "로그아웃 시 이 기기의 사본을 지운다"는 계정이 있어야 뜻이 서므로 M3 에서 함께 만든다 | 보통 — M3 에서 실제 동작을 붙인다 | M3 |
| Q-34 | **묵주 그림을 어디까지 사실적으로 그리나.** 공방장 요청("크기는 동일한 상태에서 좀 더 사실적으로")과 디자인 시스템 §9-3 의 14번("스큐어모픽 잔재가 없다 — 유리 재질 버튼, 이모지 아이콘을 쓰지 않는다")이 한 화면에서 만난다 | **§9-3 의 14번은 조작 요소(단추)에 대한 규정으로 읽고, 그림에는 적용하지 않되 그림 쪽에도 선을 그었다 — "정성껏 그린 그림"까지 가고 "번들거리는 3D 렌더"로는 가지 않는다.** 그 선의 실제 내용은 셋이다. 첫째, 광택점(하이라이트 흰 점)을 찍지 않는다. 둘째, 빛은 넓고 부드럽게 한 겹만 얹는다(무광 나무의 결). 셋째, 새 색을 들이지 않고 그 벌의 한지빛(빛)과 가장 깊은 색(그늘) 두 가지만 쓴다. 재질 셋(나무·진주·유리)으로 갈라 그리는 일은 이번 범위 밖이고, 지금 한 종은 기본값인 나무 쪽으로 잡아 두었다 | 쉬움 — `src/prayer/Rosary.tsx` 의 `MATERIAL`·`SHADING` 표 값 | **KimDesigner 검수** |
| Q-35 | **십자가가 십자가로 읽히지 않았다.** v5 의 십자가는 가로 30 · 세로 26 이라 세로보다 가로가 길어, 십자고상이 아니라 더하기 기호로 읽힌다 | **세로 길이(252~278)는 그대로 두고 가로만 17 로 좁혀 라틴 십자로 고쳤다.** 시안과 달라지는 곳이므로 검수 대상이다. 몸(코르푸스)은 새기지 않았다 — 화면에서 가로 17 · 세로 26 인 형태 안에 사람의 형상을 넣으면 형상이 아니라 얼룩이 되는 것을 실제로 그려 보고 확인했다(`docs/plan/rosary-before-after/README.md`) | 쉬움 — `Path` 하나의 `d` | **KimDesigner 검수** |
| Q-36 | **지금 알의 후광이 v5 에서 가장자리가 끊긴 원이었다.** 지름 68 짜리 원을 14% 로 통째 칠한 것이라, 빛무리가 아니라 스티커처럼 보인다. 게다가 옮긴 코드에서 그 14% 가 숨쉬기의 투명도(42~100%)로 **덮여** 있어, 동작 줄이기를 끈 기기에서는 지금 알이 지름 68 짜리 치자색 덩어리가 되고 있었다 | **두 가지를 함께 고쳤다.** 첫째, 숨의 투명도를 알 무리 전체에 한 번만 걸어 후광이 자기 몫의 14% 를 되찾게 했다(v5 는 후광에 14% 와 숨을 함께 건다 — 옮기며 잃은 값을 되돌린 것이라 시안과 어긋나지 않는다). 둘째, 그 14% 를 알의 가장자리에서 바깥으로 번지게 바꿨다 — 이쪽은 시안과 달라지므로 검수 대상이다 | 쉬움 — 그러데이션 정지점 | **KimDesigner 검수** |
| Q-37 | **아직 안 바친 알에 옅은 채움을 줄 것인가.** 채우면 실이 알 뒤로 지나가는 것처럼 보이게 만들 수 있다 | **채우지 않는다.** 실제로 10% 로 채워 렌더해 눈으로 보니 알이 유리처럼 뿌예져 §9-3 의 14번에 정면으로 걸렸고, 성화가 알 뒤로 비쳐 보이게 하려던 v5 의 뜻(`fill="none"`)도 함께 사라졌다. 대신 **테 한 줄의 짙기를 빛의 방향대로 나눠** 둥글게 보이게 했다 — 평균 짙기는 v5 값(작은 알 30% · 큰 알 34%)과 같게 잡아 그림 전체의 무게는 그대로다 | 쉬움 — `PENDING_RIM` 두 값 | **KimDesigner 검수** |


### PRD §11-1 "사람의 결정을 기다리는 것" — PRD의 기본값을 그대로 채택

| # | 결정 내용 | 채택한 기본값 + 사유 | 되돌리기 | 검토 시점 |
|---|---|---|---|---|
| D-1 | 배경 성화 확보 방법 (저작권 만료 고전 성화 / 사용 허락 현대 작품 / 새로 그림) | 검증 빌드까지는 **저작권 만료 고전 성화**(프로토타입 자산 4장은 목업 자산이므로 교체 슬롯으로 구현). 정식 출시 자산은 **비용 발생 가능(§2-5 트리거)** 이므로 M5 전에 별도 카드로 올린다 | 보통 — 이미지 슬롯 교체 | M5 전 |
| D-2 | 밤과 낮 중 기본값 | ~~두 벌을 동등 품질로 만들고 기본값은 **기기 설정 따름**으로 두어 08 검증에서 정한다~~ **닫힘 — 결정 5(2026-09-09)가 대체했다: 낮(한지) 고정.** 두 벌을 같은 품질로 만든다는 것과 밤 벌 시안이 필요하다는 것은 그대로 유효하다 | 쉬움 | 닫힘 |
| D-3 | 마침 기도 | 넣지 않는다(PRD 기본값 확정 2026-09-01) | 쉬움 | — |
| D-4 | 기도문 판본 목록과 각 판본의 사용 권한 | 검증 빌드는 **프로토타입 v4 수록문 한 판본**(`spec/prayers.ko.json`, 사도신경은 첫 문장만 — 지어 넣지 않았다)으로 진행하고, 판본 시트 S4는 자리만 잡는다. **공식 판본 전문 입력과 사용 권한 확인은 08 검증 시작 전에 마쳐야 한다**(검증 참가자가 실제로 바치므로). 권한 확인은 사람 몫 | 보통 — 데이터 파일 교체 | **M4 전 (사람 몫)** |

### §11-3 플랫폼 제약·확인 못 한 것

| 항목 | 처리 |
|---|---|
| 애플 로그인 의무(심사 4.8) | 확정 제약. FR-32대로 구글 + 애플 둘 다 |
| 54일 기도에서 하루를 건너뛰었을 때의 전통(사제 자문) | 개발과 무관하게 진행 가능. FR-35 기본값(강요하지 않음)으로 만들고, 자문 결과가 "반드시 처음부터"면 선택지를 눈에 띄게 두는 쪽으로 고친다. **사람 몫, 08 검증 전 권장** |
| 한국 앱 두 종의 실화면 · Marcello 자동 저장 여부 | 차별점 방어력 평가용. 개발에 영향 없음 |

---

## 컨펌 상태 — 관문 둘이 모두 닫혔다. 다음은 구현이다

기획안 `docs/plan/development-plan.md` §13 컨펌 체크리스트의 다섯 항목은 전부 답을 받았다.

| # | 항목 | 상태 |
|---|---|---|
| 1 | 결정 0 네 항목 | **닫힘 — 확정.** 명시적 반대가 없어 추천대로 (기획안 §13 "답이 없으면 추천대로") |
| 2 | 프레임워크·스택 (추천 A안) | **닫힘 — 결정 3 (2026-09-08).** A안 Expo + EAS Build + Supabase |
| 3 | Q-02 초대 코드 입력 자리 | **닫힘 — 결정 1-1.** 별도 화면 |
| 4 | Q-07 낭송 방식의 N 인라인 여부 | **닫힘 — 결정 1-2.** 인라인 |
| 5 | Q-08 함께 바치기 배정 화면 유무 | **닫힘 — 결정 1-3.** 만든다 |

**관문 2도 닫혔다 — 다만 통과가 아니라 대체로 닫혔다.** 관문 2는 원래 "KimDesigner가 시안을 확정·보완하고 공방장이 컨펌한다"였는데, 결정 2로 **atelier가 이미 확정한 v5 인계본이 정본 자리에 앉으면서 새로 그릴 시안이 없어졌다.** 그래서 기획안 §6-2와 §8(디자인 과제 D-A~D-O)은 수행 대상이 아니며, 그 사실을 기획안에 표시해 두었다(지우지 않았다 — v5가 덮지 못한 부분을 나중에 되짚을 때 필요하다).

**지금 열려 있는 것은 구현이다.** 다음 단계는 M0이며, KimDeveloper에게 넘길 작업 지시서는 `docs/plan/m0-work-order.md`가 정본이다. 마일스톤 전체는 기획안 §6-3이 정본이다.

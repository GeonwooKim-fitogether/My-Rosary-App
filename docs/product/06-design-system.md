> **정본 안내 (2026-09-05).** 이 파일은 atelier 저장소 `idea/mobile-rosary/06-design-system.md`(커밋 f39019b)에서 개발 착수 시점에 이관한 사본이며, **이제부터 이 저장소의 `docs/product/`가 정본**이다. atelier 쪽 원본은 freeze(더 고치지 않음)로 둔다. 개정이 필요하면 이 파일을 고치고 `decisions.md`에 개정 사유를 남긴다. 이관 경위와 문서 사이 링크 주의점은 [`README.md`](./README.md)에 있다.

# 디자인 시스템 — mobile-rosary V1

> 한 줄 요지: **이 앱의 화면이 하는 일은 "보게 만드는 것"이 아니라 "안 봐도 되게 만드는 것"이다.** 그래서 모든 결정이 한 방향을 향한다 — 곁눈질로도 읽히고, 어두운 곳에서 눈을 찌르지 않고, 끊겨도 벌주지 않는다.

이 문서는 06단계(서비스 설계)의 산출물이다. 화면을 그리기 **전에** 정해져야 하는 것들이며, 07-b 목업의 수용 기준이 된다. 어휘와 단계 정의는 [`docs/prototype-vocabulary.md`](../../docs/prototype-vocabulary.md)를 따른다.

**개정 (2026-09-05).** 공방장이 PRD 제2판을 검토해 제품 정의 아홉 곳을 정정했다([`06-prd.md`](./06-prd.md) 머리의 표). 이 문서에서 그 정정이 닿는 곳은 넷이다 — §0(첫 화면이 기도 화면이 아니라 여정 목록이 됐다), §1(한국 성화는 팔레트의 출처였을 뿐 도상의 조건이 아니게 됐다), §4-B(한복 성모 조건 삭제), §5·§9(늘어난 화면과 바뀐 수용 기준). 색 토큰 · 서체 · 시그니처(§4) · 모션 · 접근성은 그대로다.

**기준선 (2026-08-28 공방장 확정): 경쟁자를 이기는 선 — Marcello보다 세련.** Marcello의 1등 가치인 단순함은 지키되, 완성도는 Calm·Headspace 수준을 목표로 한다. 03단계 포지셔닝이 "단순·세련 UX"를 우리 자리로 정했으므로 이것은 취향이 아니라 **사업 가설**이다.

---

## 0. 먼저 구조 — 이 앱을 열면 사용자는 어디에 도착하나

색을 정하기 전에 정해야 하는 것이 있다. **앱을 열었을 때 사용자가 도착하는 곳**이다.

제2판까지 이 절의 답은 "곧바로 기도 화면"이었다. Marcello가 그렇게 하고 있고 공방장이 그 앱을 계속 쓰는 이유가 그것이었기 때문이다. **2026-09-05 공방장 정정으로 답이 바뀌었다** — 기도가 여럿일 수 있고(바람마다 여정 하나), 여정마다 54일 중 며칠째인지와 처음의 바람을 다시 보여 줘야 하므로, **홈은 여정의 목록**이다.

그렇다고 Marcello의 단순함을 버리는 것은 아니다. 지킬 것이 "화면 수"에서 **"조작 수"** 로 옮겨 갔을 뿐이다.

> **앱을 열면 내 기도(여정)의 목록이 보이고, 카드 하나를 누르면 기도 안에 있다. 목록에서 기도까지 탭 한 번.**

| 규칙 | 뜻 |
|---|---|
| 홈은 여정 카드의 목록이다 | 카드마다 **바람 · 며칠째(54일 중) · 오늘 어디까지** 가 다 적혀 있어, 카드를 열지 않고도 상태를 안다 |
| 카드를 누르면 곧 기도다 | 이어갈 자리가 있으면 그 자리부터, 없으면 오늘 것의 처음부터. 중간 화면이 없다 |
| 고르는 일은 기도 화면 밖에서 끝낸다 | 낭송 방식 · 묵주 · 기도문 판본 · 손 없이 조작은 설정과 새 기도 마법사에 있다. **기도 화면 안에는 고를 것이 하나도 없다** |
| 카탈로그 · 추천 · 캐러셀을 두지 않는다 | 목록은 내가 만든 여정만 담는다. 앱이 무엇을 권하지 않는다 |
| 탭 바를 두지 않는다 | 화면이 여덟으로 늘었지만 모든 길이 홈에서 나가 홈으로 돌아온다. 시니어가 "지금 어디 있나"를 묻지 않게 한다 |

**Hallow와 무엇이 다른가를 분명히 해 둔다.** Hallow의 홈도 목록이지만 그것은 **앱이 파는 콘텐츠**의 목록이다. 우리의 홈은 **사용자가 세운 바람**의 목록이다. 카드에 적힌 것은 상품명이 아니라 "어머니 병환 회복 · 23일째 · 청원 중"이다.

---

## 1. 어디서 가져왔나 — 이 제품의 세계

**v1 팔레트("밤의 성당 — 보랏빛 어둠, 촛불 금빛, 제대포 흰색")를 폐기하고 여기서 다시 쓴다.** 폐기 사유는 조사로 확인됐다. 그 팔레트의 출처는 서양 성당의 밤 이미지였고, 근거는 조사가 아니라 짐작이었다. 한국 가톨릭에는 이미 확립된 색 언어가 따로 있다.

조사가 찾아낸 것은 한 문장으로 요약된다. 한국 가톨릭 미술을 설명하는 말이 **"천연염색 같은 은은하고 부드러운 색채"** 다. 근거는 [`02b-design-research.md`](./02b-design-research.md) §2에 있다.

**여기서 가져오는 것은 색이고, 그림이 아니다.** 제2판은 같은 조사에서 "한복 성모 · 동글동글한 얼굴"이라는 도상까지 가져와 차별점으로 세웠는데, 2026-09-05 공방장 정정으로 그 도상 조건은 빠졌다. 팔레트는 남는다 — 쪽 · 한지 · 치자는 화면의 색이고, 배경에 어떤 성화를 놓을지와는 별개의 결정이다(§4-B).

| 이 제품의 세계에 있는 것 | 디자인으로 옮긴 것 |
|---|---|
| **쪽(藍)물 들인 천** — 검정이 아니라 깊은 남색 | 어두운 화면의 바탕 (`#10161F`) |
| **한지(韓紙)** — 순백이 아닌, 빛을 머금은 종이 | 기도문 글자색이자 밝은 화면의 바탕 (`#F0EAD9`) |
| **치자물** — 형광 없이 밝은 따뜻한 노랑 | 주 강조. "지금 일어나는 일" (`#D9AE4C`) |
| **전례색** — 시기마다 이미 뜻이 정해진 색 | 부 강조. 고정값이 아니라 **시기에 따라 갈아 끼우는 슬롯** (§2-3) |
| **묵주 알** | 화면의 주인공 (§4) |

**형태의 원리도 함께 가져온다.** 천연염색의 특징은 색 이름이 아니라 **낮은 채도와 부드러운 경계**다. 그래서 이 앱에는 순색이 없고, 형광이 없고, 날카로운 모서리가 없다. 원과 곡선을 쓰고, 경계는 단면이 아니라 번짐에 가깝게 둔다.

---

## 2. 색 토큰

### 2-1. 위계를 무엇으로 만드나 — 채도가 아니라 명도

이 결정이 팔레트 전체의 성격을 정하므로 먼저 적는다.

Hallow의 화면은 위계를 **채도**로 만든다. 어두운 검정 크롬 위에 채도 높은 플랫 벡터 일러스트를 얹어, 색이 진한 쪽이 주인공이 된다. 그런데 우리의 천연염색 팔레트는 정의상 채도가 낮으므로 **같은 수법을 쓸 수 없다.** 낮은 채도끼리 나란히 놓으면 아무것도 튀지 않고 화면이 죽는다.

그래서 우리는 위계를 **명도(밝기)** 로 만든다. 어두운 바탕 위에서 **가장 밝은 것이 주인공**이고, 그 순서가 곧 읽는 순서다.

동시에 Hallow에게서 배울 것은 하나 남는다 — **어두운 것은 액자이고, 그림은 밝아야 한다.** 내 v3가 실패한 기술적 원인이 여기였다. 어두운 크롬 위에 어두운 콘텐츠를 얹어 액자와 그림의 밝기가 같았고, 그래서 아무것도 주인공이 되지 못했다.

| 명도 층 | 무엇이 여기 오나 |
|---|---|
| 1층 (가장 밝음) | 알이 "내 차례"로 숨 쉴 때, 지금 읽는 기도문 |
| 2층 | 본문 글자, 큰 버튼의 글자 |
| 3층 | 보조 설명, 지난 알 |
| 4층 (가장 어두움) | 바탕, 아직 오지 않은 알 |

### 2-2. 두 벌을 만든다 — 밤이 기본이다

주 사용 맥락이 **잠자기 전 어두운 방**이므로(01·02단계 조사) 어두운 화면을 기본값으로 둔다.

**다만 이 전제는 아직 검증되지 않았음을 밝혀 둔다(결정 큐).** 01단계가 사용 맥락을 밤으로 특정한 것은 근거가 있으나, 거기서 "그러므로 화면이 어두워야 한다"가 자동으로 따라오지는 않는다. 프랑스 앱 Rosario는 **거의 흰 바탕에 부드러운 색 번짐**으로 같은 시장에서 성립하고 있고, 세 경쟁자 중 가장 현대적으로 보인다(조사 §1-4 라). 그래서 **두 벌을 동등한 품질로 만들고, 어느 쪽을 기본값으로 할지는 08단계 사용자 검증으로 정한다.** 지금의 기본값은 잠정이다. 다만 그 어둠은 검정이 아니라 **쪽물이 가장 깊게 든 남색**이다. 낮에 쓰는 사람과 시니어를 위해 한지 바탕의 밝은 벌도 함께 만든다.

```css
/* ── 밤 (기본) — 쪽물에 담근 어둠 ─────────────── */
--ground:   #10161F;  /* 바탕 — 쪽빛이 가장 깊게 든 곳. 검정이 아니다 */
--surface:  #18202C;  /* 표면 — 카드·시트 */
--surface-2:#222C3B;  /* 표면 강조 — 선택된 것 */
--hanji:    #F0EAD9;  /* 기도문·본문 글자 — 한지. 순백이 아니다 */
--chija:    #D9AE4C;  /* 주 강조 — 치자물. 지금 일어나는 일 */
--chija-hi: #F2DCA0;  /* 알이 숨을 들이쉰 순간의 최고 밝기 */
--dusk:     #8E93A3;  /* 보조 글자 */
--ash:      #4A5262;  /* 비활성·아직 오지 않은 알 */
--line:     rgba(240,234,217,.10);

/* ── 낮 — 한지 ────────────────────────────────── */
--ground:   #EDE7D8;
--surface:  #F5F1E6;
--surface-2:#E2DAC7;
--ink:      #1F2530;  /* 먹(墨) — 글자 */
--chija-d:  #82600F;  /* 밝은 바탕에서의 주 강조. 2026-09-08 개정 — 문서 끝 개정 이력 참조 */
                      /* 원래 값은 #8A6516 이었으나 본문 크기 글자에서 대비가 3.9:1로 떨어져
                         §7 의 4.5:1 기준에 미달했다. 조정한 #82600F 는 실측 4.7:1 이다 */
--dusk-d:   #5C6272;
--ash-d:    #A9A395;
```

### 2-3. 전례색은 고정값이 아니라 갈아 끼우는 슬롯이다

**Edge point 2가 여기서 코드가 된다.** 부 강조색을 하나로 고정하지 않고 `--season` 슬롯을 두어, 전례 시기에 따라 값이 바뀐다. 신자가 이미 아는 언어이므로 장식이 아니라 정보다.

| 시기 | 전례색 | 밤 팔레트 값 | 무엇에 쓰나 |
|---|---|---|---|
| 대림·사순 | 자색 | `#7A6699` | 신비 표시, 단 구분선, 진행 테두리 |
| 연중 | 녹색 (쑥빛) | `#7E8B6B` | 〃 |
| 부활·성탄·성모 축일 | 백색 | `#E4DED0` | 〃 |
| 성령강림·수난·순교 | 홍색 | `#9E4A57` | 〃 |

**주의 하나.** 아래 §8의 "초록을 성공 표시로 쓰지 않는다"는 규칙은 그대로 살아 있다. 여기의 쑥빛 녹색은 성공이 아니라 **연중 시기**를 뜻하며, 둘은 다른 것이다. 같은 색이 두 뜻을 갖지 않도록, 완주 표시에는 어떤 경우에도 녹색을 쓰지 않는다.

### 2-4. 쓰지 않기로 한 것

| 쓰지 않는다 | 왜 |
|---|---|
| 순색·형광 | 천연염색의 성격과 정면으로 어긋난다 |
| 붉은 경고색 | 기도에서 붉은 경고는 벌주는 인상을 준다. 주의는 치자색으로 표시한다 |
| 초록의 **성공** 용법 | 완주는 성취가 아니라 마침이다 (전례색 녹색과 혼동 금지) |
| 채도 높은 플랫 벡터 일러스트 | Hallow·Rosario의 어휘다. 따라가면 자본 싸움이 되고, 우리 팔레트와도 어긋난다 |
| **치자색의 그라데이션 사용** | Rosario의 오렌지→앰버 그라데이션 알약 버튼과 겹친다. 치자는 단색으로만 쓴다 |

## 3. 타이포그래피 — 두 얼굴을 쓴다

**기도문과 조작 버튼은 다른 성질의 글이다.** 같은 서체로 쓰면 기도문이 앱 UI처럼 읽힌다.

| 역할 | 서체 | 어디에 |
|---|---|---|
| **기도문** | `Noto Serif KR` (400/600) | 성모송·주님의 기도·신비 선포. 세리프는 전례서의 활자다 |
| **조작·표시** | `Noto Sans KR` (400/500/700) | 버튼·라벨·상태 |

**크기 계단** — 시니어(퍼소나 B)가 기준이다. 본문 최소 16px, 기도문은 훨씬 크게.

| 이름 | 크기 / 행간 | 어디에 |
|---|---|---|
| `prayer-lg` | 26px / 1.7 | 앱이 읽는 앞 절 |
| `prayer-md` | 21px / 1.7 | 사용자가 받을 뒷 절 |
| `title` | 20px / 1.4 | 오늘의 신비 |
| `body` | 16px / 1.6 | 기본 |
| `label` | 13px / 1.5 | 버튼·지향 이름 |
| `caption` | 11.5px / 1.5 | 보조 설명 |

시스템 글자 크기 확대(iOS Dynamic Type)를 따른다. 웹에서는 `rem` 기준.

## 4. 시그니처 — 실제 묵주의 형태 위에서, 지금 알 하나가 숨을 쉰다

**이 절은 Marcello 실화면을 보고 전면 개정했다.** 개정 전의 설계는 화면 한가운데 큰 원 하나를 두고 그 원이 호흡하게 하는 것이었는데, 그것이 왜 열등한지가 실물 비교로 드러났다.

### 4-1. 알의 배치 — 정원(正圓)을 버리고 실제 묵주 형태로

내 v3는 알을 **정원 위에 균등 배치**했다. Marcello는 **실제 묵주의 모양** — 위가 벌어진 고리에 아래로 십자가와 알 몇 개가 늘어진 형태 — 를 그렸다. 셋을 비교하면 우리가 왜 졌는지가 분명하다.

| | v3 정원 배치 | 실제 묵주 형태 |
|---|---|---|
| 손과의 대응 | 없다. 손에 든 묵주와 모양이 다르다 | **있다.** 형태가 같아 눈으로 확인할 일이 준다 |
| 주님의 기도 알 | 전부 같은 크기라 구조가 안 보인다 | **큰 알**로 고리에서 구조적으로 튀어나온다 |
| 시작점과 순서 | 원에는 시작점이 없다 | 십자가에서 꼬리를 지나 고리로 오르는 **순서가 형태에 들어 있다** |

**결정: 실제 묵주의 형태를 쓴다.** 이것은 Marcello의 발명이 아니라 묵주 그 자체의 형태이므로 베끼는 것이 아니라 대상을 제대로 보는 일이다. 다만 알의 크기는 Marcello보다 키운다 — 그쪽은 알 지름이 화면 폭의 약 5%라 시니어가 짚기 어렵다(조사 §1-3 마-6).

**2026-09-09 에 "실제 묵주의 형태"가 무엇인지가 한 번 더 좁혀졌다** (`decisions.md` 결정 8). 구현은 이 절을 "위가 벌어진 고리"로 읽어 **납작한 타원**을 그렸는데, 공방장이 보낸 실제 묵주 사진 넉 장은 어느 것도 타원이 아니었다. 셋을 못 박아 둔다.

1. **고리는 아래 중심 메달로 모이는 물방울이다.** 위쪽에서 두 어깨가 넓게 벌어지고, 아래로 내려오면서 좁아져 메달 한 점에서 만난다. 두 가닥은 메달에서 거의 곧게 서서 갈라진다(구현 실측 84도).
2. **알과 알 사이에 줄이 보인다.** 알이 서로 붙어 있으면 구슬을 꿴 것이 아니라 굵은 선으로 보인다.
3. **단과 단 사이의 줄이 한 칸 더 길다.** 각 단의 경계에서 큰 알 하나가 긴 줄 두 도막 사이에 홀로 놓이고, 그것으로 다섯 단이 눈으로 갈라진다.

셋을 지키려면 알을 작게 그려야 한다(둘레가 정해져 있는데 알과 줄이 그 안에 함께 들어가야 한다). 그래서 §9-3 의 13번이 요구하는 "화면 폭의 8% 이상"은 **지금 바치는 알**에 대한 요구로 읽는다 — 나머지 알은 "얼마나 남았나"를 말하고, 짚어야 하는 알은 언제나 지금 알 하나뿐이다.

### 4-2. 그 위에서 지금 알 하나가 숨을 쉰다 — 우리만 할 수 있는 것

화면이 전달해야 하는 것은 두 가지다. **어디쯤인가**와 **지금 내 차례인가.**

앞의 것은 위 형태가 푼다. **뒤의 것은 두 경쟁자 모두 답이 없다** — 둘 다 낭송이 오디오 재생 한 방향이라 "내 차례"라는 상태 자체가 없기 때문이다. 우리의 1순위 USP인 **교대 낭송**이 이 상태를 만들어 내고, 그래서 이 신호는 우리만 가질 수 있다.

거대한 별도의 원을 따로 두지 않는다. **묵주 고리 위의 현재 알 하나가 커지면서 숨을 쉰다.**

| 상태 | 현재 알이 하는 일 |
|---|---|
| 앱이 앞 절을 읽는 중 | 알의 테두리가 시계 방향으로 차오른다. 속은 어둡다 |
| **내가 받을 차례** | 알이 커지면서 **숨을 쉰다** — 4초 주기로 치자색이 밝아졌다 어두워진다 |
| 침묵 진행 | 알은 그대로 있고 진동에 맞춰 한 번 반짝인다 |
| 단이 바뀔 때 | 큰 알(주님의 기도)로 초점이 옮겨 가며 한 번 확장한다 |
| 지나간 알 | 한지색으로 조용히 채워진다. 광채는 두르지 않는다 |

이 하나가 셋을 동시에 푼다. **첫째,** "받아 바치세요" 같은 글자가 필요 없다 — 글자는 봐야 읽히지만 밝기 변화는 곁눈질로도 안다. **둘째,** 커진 알은 시니어도 짚을 수 있다. **셋째,** 눈을 감고 있어도 눈꺼풀 너머 밝기로 차례를 느낀다. 잠자기 전 사용이 주 맥락이므로 이것이 실제로 작동한다.

**대담함은 여기 한 곳에만 쓴다.** 나머지 화면은 전부 조용히 둔다.

### 4-3. 묵주 재질 넷 — 무엇으로 만든 묵주인가

FR-39 는 "서로 확실히 다른 묵주 셋 이상"을 요구하고 §9-3 의 22번은 "고른 묵주가 알의 다섯 상태를 전부 표현한다"를 요구한다. 그 요구를 채우는 재질 넷을 2026-09-09 에 정했다(`decisions.md` 결정 9). 값은 공방장이 보낸 실제 묵주 사진 넉 장에서 왔다.

**재질이 바꾸는 것은 셋뿐이다** — 알의 빛깔과 광택, 줄의 생김새(끈인가 사슬인가), 십자가와 메달의 빛깔. **알의 자리와 크기는 넷이 모두 같고, 지금 바치는 알의 치자색도 재질을 따르지 않는다.** 치자색은 재질이 아니라 §4-2 가 정한 **상태**를 말하는 색이기 때문이며, 그래서 어느 묵주를 골라도 "지금 어디인가"가 같은 세기로 보인다.

| 키 | 이름 | 사진에서 본 것 | 줄 | 광택 |
|---|---|---|---|---|
| `rose` | 붉은 장미 | 붉은 장미꽃으로 조각된 알. **기본값이다** | 검은 끈 마디 | 0.45 |
| `wood` | 나무 | 짙은 갈색 나무 알인데 주님의 기도 큰 알만 밝은 살구빛이라 눈에 띈다 | 크림빛 매듭 끈 | 0.25 (무광) |
| `silver` | 은 | 은빛 금속 구슬, 빛이 한 점에 또렷하게 맺힌다 | 은 사슬 고리 | 0.90 |
| `gold` | 금 | 금빛 금속 구슬, 은보다 따뜻하고 반사가 넓다 | 금 사슬 고리 | 0.75 |

"광택"은 빛이 얼마나 좁게 맺히는가를 0~1 로 적은 값이다. 이 값 하나가 빛 그러데이션의 반지름과 퍼짐을 함께 정하므로, 금속 구슬과 무광 나무 알의 차이가 값 하나로 갈린다.

**색 값은 낮 벌과 밤 벌을 따로 둔다.** 금과 은은 밝은 한지 위에서 흐려지고 나무와 장미는 어두운 쪽빛 위에서 묻히기 때문이다. 낮은 골동품처럼 가라앉힌 금속을, 밤은 빛을 머금은 금속을 쓴다. 옮기는 규칙은 하나다 — **색상(무슨 색인가)은 그대로 두고 밝기만 옮긴다.** 그래야 밤에도 "이건 나무구나"가 유지된다.

| 재질 | 낮: 작은 알 · 큰 알 · 줄 · 금속 | 밤: 작은 알 · 큰 알 · 줄 · 금속 |
|---|---|---|
| 붉은 장미 | `#8E2733` · `#6E1C27` · `#2A2732` · `#8A6A22` | `#DE6A7E` · `#E9909E` · `#9A96A6` · `#E0BE72` |
| 나무 | `#4E3423` · `#9A6234` · `#6B5A44` · `#6B4830` | `#A87C55` · `#DCB183` · `#CBB79A` · `#C79A6E` |
| 은 | `#767E8D` · `#5C6472` · `#767E8D` · `#767E8D` | `#C3C9D5` · `#DDE2EA` · `#C3C9D5` · `#C3C9D5` |
| 금 | `#9F7518` · `#7E5C11` · `#9F7518` · `#9F7518` | `#E0BE72` · `#EDD394` · `#E0BE72` · `#E0BE72` |

**대비 기준을 여기서 갈라 둔다.** §7 이 정한 4.5:1 은 **본문 글자**의 기준이고, 묵주 알은 글자가 아니라 그림 요소다. 그래서 알과 줄과 금속은 바탕과 **3:1** 을 넘기고, 지금 알 위에 적히는 숫자만 **4.5:1** 을 넘긴다. 알에 4.5:1 을 요구하면 금과 은을 치자색만큼 어둡게 낮춰야 하고, 그러면 금 묵주와 지금 알이 같은 색이 되어 오히려 상태가 흐려진다. 가장 빠듯한 짝은 낮 벌의 은(3.31:1)과 금(3.38:1)이며, 값이 실제로 그 기준을 넘는지는 눈이 아니라 `src/prayer/rosaryMaterials.test.ts` 가 계산해 지킨다. 판단의 대가는 `decisions.md` 결정 큐 Q-43 에 적었다.

**아직 안 바친 알에도 재질의 빛깔을 준다.** 고리의 알 대부분은 늘 이 상태이므로, 여기가 흐리면 재질을 고른 보람이 없다. 채우지는 않고(§9-3 의 14번과 v5 의 뜻을 지킨다) 그 재질의 알 빛깔로 테만 두르되, **온전한 짙기로** 두른다.

---

## 4-B. 도상 — 성모님을 어떻게 화면에 모실 것인가

조사에서 가장 크게 바뀐 대목이다. 나는 "우리 화면에 성모님이 0장인 것이 문제이고 그 자리가 비어 있다"고 판단했는데, **Marcello가 이미 그 자리를 채우고 있었다.** 화면 전체가 성화이고, 신비가 바뀌면 그림도 바뀐다.

그러므로 우리의 질문은 "성모님을 넣을 것인가"가 아니라 **"어떤 성모님인가"** 다.

### 4-B-1. 배경 성화를 쓴다 — Marcello의 구조는 옳다

두 가지를 그대로 가져온다. 이것은 이미 검증된 좋은 구조다.

1. **배경 전체가 성화 한 장이다.** 사용자가 묵상할 대상이 화면에 있다.
2. **신비가 바뀌면 그림이 바뀐다.** 지금 무엇을 묵상하는지가 글자가 아니라 그림으로 전달된다.

### 4-B-2. 도상의 출처는 이 문서가 정하지 않는다 — 화질과 구도만 정한다

제2판은 여기에 "한국 성화 — 한복 성모, 동글동글한 얼굴"을 적었다. **2026-09-05 공방장 정정 1·6으로 그 조건은 삭제됐다.** 어떤 성화를 놓을지는 저작권·비용과 함께 PRD D-1이 정하고, 이 문서는 어떤 성화가 오더라도 지켜야 하는 것만 적는다.

| | Marcello | 우리 |
|---|---|---|
| 출처 | 서양 19세기 성화·이탈리아 성물 이미지 | **미정 (PRD D-1).** 07-b 목업은 저작권이 만료된 고전 성화로 진행한다 |
| 화질 | 저해상도를 늘려 써 뭉개진다 | 원본 해상도로 선명하게. 확보하지 못한 그림은 쓰지 않는다 |
| 구도 | 얼굴 위에 묵주가 얹힌다 | 인물의 이목구비 영역을 비우고 묵주는 그 아래에 둔다 (§4-B-3) |
| 신비와의 대응 | 신비마다 다른 그림 | 같다 — 이것은 Marcello가 옳다 |

### 4-B-3. Marcello가 저지른 실수 셋을 피한다

실화면에서 직접 확인한 결함이며(조사 §1-3 마), 이것을 피하는 것이 **"Marcello보다 세련"의 실제 내용**이다.

| 그쪽의 실수 | 우리 규칙 |
|---|---|
| 십자가가 성모님 코 위에, 파란 알이 **눈동자 위에** 얹힌다 | **묵주는 성모님의 이목구비를 절대 가리지 않는다.** 고리가 얼굴을 감싸되 눈·코·입 영역은 비운다. 목업 검수 항목으로 박는다 |
| 기도문 띠 높이가 글자 길이에 따라 변해 **그림이 잘리는 양이 매번 달라진다** | 기도문 영역의 높이를 **고정**하고, 긴 기도문은 그 안에서 처리한다. 그림의 구도가 화면마다 흔들리지 않는다 |
| 배경 성화의 해상도가 낮아 뭉개진다 | 원본 해상도를 확보하지 못한 그림은 **쓰지 않는다** |

### 4-B-4. 아직 정해지지 않은 것 — 결정 큐로 올린다

**현존 작가의 작품에는 저작권이 있다.** 조사가 찾아낸 것은 "그 화풍이 어떤 것인가"이지 "그 그림을 쓸 수 있다"가 아니다. 선택지는 셋이며, 비용과 시간이 갈리므로 **여기서 임의로 정하지 않는다.**

| 선택지 | 성격 |
|---|---|
| 작가·교구와 사용 허락을 협의한다 | 정통성이 가장 높다. 시간과 비용이 든다 |
| 팔레트의 **원리**(낮은 채도, 부드러운 경계)를 따라 우리가 새로 그린다 | 저작권 문제가 없다. 품질이 우리 손에 달린다 |
| 저작권이 만료된 한국 성미술과 공공 도상을 찾는다 | 비용이 없다. 확보 가능한 양이 불확실하다 |

**07-b 목업 단계에서는 세 번째(저작권 만료 성화)로 진행하고, 실제 출시 자산은 공방장의 결정을 받는다.** 2026-09-05 정정으로 "한국 성화"라는 조건이 빠져 세 번째 선택지의 폭이 크게 넓어졌다 — 이것이 기본값을 세 번째로 옮긴 이유다. 이 항목은 비용이 발생할 수 있으므로 에스컬레이션 규칙 §2-5(비용 발생)에 해당한다.

---

## 5. 컴포넌트와 상태

| 컴포넌트 | 상태들 |
|---|---|
| 큰 버튼 | 기본 · 눌림 · 비활성 · 로딩 |
| 로그인 단추 (구글 · 애플) | 기본 · 눌림 · 진행 중. 각 제공자의 브랜드 지침을 따르되 우리 팔레트 위에 놓는다 |
| **여정 카드** (홈의 단위) | 진행 중 — 오늘 아직 / 오늘 이어갈 자리 있음 / 오늘 바쳤음 · **마침** · 조 기도(내 몫과 조 진척이 함께) |
| **여정 진척** | 54일 기도 — 며칠째(N/54)와 청원/감사. 배지·불꽃·백분율 링으로 그리지 않는다. **하루가 한 칸인 가로 리본**이고 빈 날은 비어 있는 채로 둔다 |
| 마법사 걸음 (새 기도 시작) | 1/3 · 2/3 · 3/3. 걸음마다 질문 하나 |
| 설정 항목 | 선택형(낭송 방식 셋 · 묵주 · 판본) · 켬/끔(손 없이 조작 · 밤/낮) · 실행(로그아웃 · 계정 삭제 — 후자는 확인 시트) |
| 알 (시그니처) | 대기 · 읽는 중 · 내 차례 · 침묵 · 단 전환 |
| 알 열 개 | 지난 알 · 지금 알 · 남은 알 |
| 시트 | 닫힘 · 열림 |
| 빈 상태 | 지향이 없을 때 · 진행이 없을 때 |

**빈 상태는 방향을 주는 자리다.** "아직 없습니다"로 끝내지 않고 다음에 무엇을 하면 되는지 적는다.

## 6. 모션

| 무엇 | 시간 | 곡선 |
|---|---|---|
| 화면 전환 | 500ms | `cubic-bezier(.4,0,.2,1)` |
| 알의 호흡 | 4000ms 주기 | `ease-in-out` 왕복 |
| 글자 교체 | 450ms | `ease` |
| 눌림 반응 | 150ms | `ease-out` |

02단계 조사가 확인한 값이다 — 명상 앱은 일반 앱(250~300ms)보다 느린 400~600ms를 쓰고, 그 느림 자체가 진정 효과를 낸다. **`prefers-reduced-motion`을 켠 사용자에게는 호흡을 멈추고 밝기만 바꾼다.**

## 7. 접근성 — 협상 대상이 아닌 것

| 항목 | 기준 | 왜 |
|---|---|---|
| 터치 영역 | 최소 **80px** (문서 끝 개정 이력의 Q-16 행을 함께 본다) | 02단계의 졸음 상태의 부정확한 손가락. 여기서 px 은 코드가 쓰는 단위(iOS 의 pt, Android 의 dp)이며 기기 픽셀이 아니다. 그 단위에서 80 은 iOS 가 권고하는 44 와 Android 가 권고하는 48 을 모두 넉넉히 넘긴다 |
| 본문 대비 | 4.5:1 이상 | 어두운 화면에서도 읽혀야 한다 |
| 큰 글자 대비 | 3:1 이상 | |
| 시스템 글자 확대 | 200%까지 깨지지 않는다 | 퍼소나 B 시니어 |
| 색만으로 상태를 알리지 않는다 | 밝기·크기·진동을 함께 쓴다 | |

## 8. 톤 — 쓰지 않는 말

06단계 서비스 설계에서 이미 정한 것을 디자인 규칙으로 못 박는다.

| 쓰지 않는다 | 대신 |
|---|---|
| 실패 · 놓침 · 연속 기록이 끊겼습니다 | 이어서 바치기 · 그 자리부터 |
| 축하합니다 · 달성 | 다 바쳤습니다 |
| 붉은 경고색 | 치자색으로 주의를 표시 |
| 순위 · 비교 · 퍼센트 배지 | 바친 횟수만 조용히. 54일 진척은 **"23일째 · 청원"** 처럼 위치로 말한다 |
| 빼먹었습니다 · 연속 N일 | 빈 날은 리본에 비어 있는 칸으로만 남긴다. 말을 붙이지 않는다 |

## 9. 수용 기준 — 목업이 통과해야 하는 것

07-b 목업은 아래를 전부 만족해야 07-d로 넘어간다. 2026-08-30 실화면 분석 뒤 항목을 늘렸고, 2026-09-05 정정으로 다시 늘렸다(24항목). **2026-08-30에 늘린 것은** — 늘어난 것은 전부 경쟁사 화면에서 실제로 확인한 결함을 막는 조항이다.

**2026-09-05 개정** — 홈이 여정 목록이 되고 화면이 여덟으로 늘었으므로 1·2·8번을 고치고 19~22번을 더했다.

### 9-1. 기본 요건 (어기면 지는 것 — 조사 §4-3)

1. **홈의 여정 카드에서 기도 시작까지 탭 한 번.** 카드와 기도 화면 사이에 다른 화면이 없다.
2. 오늘의 신비를 앱이 정해 알려 준다 — 자유 기도는 요일, 54일 기도는 순환 규칙. 고르는 자리가 없다.
3. 알을 **실제 묵주의 형태**로 그렸다. 정원 균등 배치가 아니다.
4. 신비가 바뀌면 배경 성화가 바뀐다.
5. 광고·팝업·순위·업셀이 0이다.

### 9-2. 차별점 (이기러 가는 것)

6. 위 색·서체·크기 계단을 그대로 썼다 (임의의 값이 없다).
7. 현재 알의 다섯 상태가 전부 그려져 있다 (§4-2).
8. 배경 성화가 있고 신비마다 바뀐다. 출처는 PRD D-1의 범위 안이다(목업은 저작권 만료 성화).
9. `--season` 전례색 슬롯이 실제로 값을 갈아 끼울 수 있게 되어 있다.

### 9-3. Marcello의 결함을 피했는가 (실화면 대조 항목)

10. **묵주가 성모님의 이목구비를 가리지 않는다.** 눈·코·입 영역에 알이나 십자가가 겹치지 않는다.
11. **기도문 영역의 높이가 고정**이다. 기도문 길이가 달라져도 배경 그림이 잘리는 양이 변하지 않는다.
12. 배경 성화가 늘어나 뭉개지지 않는다 (원본 해상도 확보).
13. 알의 지름이 화면 폭의 **8% 이상**이다 (Marcello는 약 5%로 시니어가 짚기 어렵다).
14. 스큐어모픽 잔재가 없다 — 유리 재질 버튼, 이모지 아이콘을 쓰지 않는다.
15. 설정 메뉴에 위계가 있다. 평평한 열여섯 줄 목록이 아니다.

### 9-4. 접근성·실측

16. 터치 영역 **80px**(단위는 pt·dp — 개정 이력 Q-16), 본문 대비 4.5:1을 **실측으로** 만족한다.
17. 빈 상태 두 개가 그려져 있고, 각각 다음에 할 일을 말한다 (Hallow는 두 탭이 같은 빈 화면이다).

### 9-5. 여정 · 계정 · 조 (2026-09-05 추가)

19. 홈 카드 하나에 **바람 · 며칠째(54일 중) · 오늘 어디까지** 가 다 적혀 있다. 카드를 열어야 아는 것이 없다.
20. 54일 진척이 배지·불꽃·백분율 링이 아니라 **하루 한 칸 리본**이고, 빈 날이 비어 있는 채로 그려져 있다.
21. 새 기도 시작이 **세 걸음**이고 걸음마다 질문이 하나다. 완료 예정일이 마지막 걸음에 보인다.
22. 묵주 고르기 시트에 서로 확실히 다른 묵주가 **셋 이상** 있고(지금은 넷 — §4-3), 고른 묵주가 알의 다섯 상태(§4-2)를 전부 표현한다. 시트에는 고른 묵주의 **큰 미리보기**가 함께 서서, 그 다섯 상태를 고르기 전에 볼 수 있다.
23. 기도 화면 안에 **고르는 조작이 하나도 없다** (낭송 방식 · 묵주 · 판본 · 손 없이 조작은 전부 밖에 있다).

### 9-6. 최종

24. **Marcello와 나란히 놓았을 때 더 세련돼 보인다.** 이 판정은 공방장이 한다.

---

## 개정 이력 (My-Rosary-App 이관 뒤)

이 문서는 atelier 저장소에서 만들어져 2026-09-05 에 이 저장소로 이관됐다. 이관 뒤 정해진 것이 본문의 어느 값을 대체하는지를 아래에 적는다. 본문의 해당 줄에는 이 표로 오는 안내를 함께 달아 두었으므로, 본문만 읽고 옛 값을 그대로 쓰는 일이 없도록 했다. 근거는 모두 저장소 루트의 `decisions.md` 에 있다.

| 언제 | 무엇이 바뀌었나 | 이 문서의 어느 부분인가 | 근거 |
|---|---|---|---|
| 2026-09-08 | **터치 영역의 최소 기준은 80px 그대로 둔다.** 한때 88px 로 올리기로 했으나(Q-06b), 그 88 이라는 숫자는 iOS 의 44pt 를 2배 화면의 *기기 픽셀* 로 환산해 나온 값이었다. 코드와 이 문서가 쓰는 단위는 기기 픽셀이 아니라 pt·dp 이고, 그 단위에서 80 은 이미 iOS 44 와 Android 48 을 모두 넘는다. 즉 Q-06b 가 내세운 "두 플랫폼 권고를 모두 넘긴다"는 목적을 80 이 이미 만족한다. v5 시안 37 곳과 구현 코드가 모두 80 이므로 그것을 정본으로 삼는다 | §7 접근성 표의 터치 영역 행, §9-4 수용 기준 16번 | `decisions.md` Q-16 |
| 2026-09-08 | **낮 벌의 주 강조색 `--chija-d` 가 `#8A6516` 에서 `#82600F` 로 바뀌었다.** 원래 값은 한지 바탕 위 본문 크기 글자에서 명도 대비가 3.9:1 로 떨어져 이 문서 §7 이 스스로 정한 4.5:1 기준을 지키지 못했다. 조정한 값은 실측 4.7:1 이다 | §2-2 낮 벌 팔레트의 `--chija-d` 선언 | `decisions.md` Q-09 |
| 2026-09-09 | **묵주 고리의 모양이 타원에서 물방울로 바뀌었다.** §4-1 이 "위가 벌어진 고리"라고만 적어 두어 구현이 납작한 타원을 그렸는데, 실제 묵주 사진 넉 장이 그것과 달랐다. §4-1 에 셋(물방울 · 알 사이의 줄 · 단 사이의 긴 줄)을 못 박았다 | §4-1 | `decisions.md` 결정 8 |
| 2026-09-09 | **묵주 재질 넷이 값으로 정의됐다.** 그전까지 이 문서에는 재질의 색 값이 없었고, 구현도 고른 묵주를 읽지 않아 그림이 바뀌지 않았다. §4-3 이 새로 생겼고, 알의 대비 기준을 §7 의 4.5:1(글자)과 3:1(그림 요소)로 갈랐다 | §4-3 (새 절), §9-3 의 22번 | `decisions.md` 결정 9 · 결정 큐 Q-43 |
| 2026-09-08 | **화면 시안의 정본이 이 문서가 아니라 `docs/design/v5/` 로 옮겨 갔다.** 이 문서는 여전히 색·서체·모션·접근성의 *원리*를 정하는 정본이지만, 실제 화면에 쓰인 구체적인 수치(간격·크기·배치)가 이 문서와 v5 시안에서 어긋나는 경우에는 **v5 시안을 따른다** | 문서 전체에 걸친 우선순위 규정 | `decisions.md` 결정 2 |

**아직 이 문서가 그리지 않은 것이 둘 있다.** 하나는 바텀 시트 일곱(S2~S7 과 계정 삭제 확인)이고 다른 하나는 밤 벌(쪽빛 테마)의 실제 화면이다. 색 토큰은 §2-2 에 두 벌이 모두 정의돼 있으나 그 토큰으로 그린 화면 시안은 없다. 이 둘은 시안을 새로 그리지 않고, v5 가 확립한 낮 벌의 값 체계에서 구현자가 파생해 코드로 먼저 만들고 KimDesigner 가 렌더된 화면을 검수하는 방식으로 처리한다. 착수 시점은 M2 다. 근거는 `decisions.md` Q-14 다.
